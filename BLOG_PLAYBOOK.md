# دليل بناء مدونة zero2one — من الصفر إلى الختام

> **الغرض:** أن تُبنى مدونة ثانية بهذه الطريقة بلا أن يشرح لك أحد شيئاً.
> كل قرار هنا مكتوب بسببه، وكل عطل بجذره لا بعَرَضه.
>
> **تاريخ التوثيق:** 2026-08-09 · **المدى:** 2026-08-02 ← 2026-08-09
> **الالتزامات الموثَّقة:** 39 في `blog-build` · 25 في `blog-content`
> **العاملون:** Claude و Codex — الوثيقة لا تفرّق، فالنتيجة واحدة.

---

## ٠ · الخلاصة في دقيقة

مدونة Hugo ثنائية اللغة تعيش على `zero2one.sa/blog/`، تُنشر عبر **مستودع الموقع** لا FTP، ويكتبها غير التقني من لوحة `Sveltia CMS`. سبعة عشر فاحصاً آلياً تمنع النشر عند أي خلل، وثلاثة مستودعات تفصل **القوالب** عن **المحتوى** عن **الموقع**.

| | |
|---|---|
| المولّد | Hugo 0.164.0 extended — **مثبّت ببصمة sha256** |
| اللغات | إنجليزي على `/blog/` · عربي على `/blog/ar/` |
| النشر | GitHub Actions ← مستودع الموقع ← مزامنة Hostinger |
| اللوحة | Sveltia CMS مستضاف ذاتياً + وسيط OAuth على Cloudflare |
| البوابات | 17 أداة فحص — أي فشل يوقف النشر |

---

## ١ · لماذا ثلاثة مستودعات

```
zeroone01z21-alt/blog-build     القوالب والأدوات والضوابط  ← لا محتوى فيه
zeroone01z21-alt/blog-content   مقالات الكاتب وصورها       ← لا كود فيه
01team9639-maker/zero2one-web   الموقع + مجلد blog/ المبني ← وجهة النشر
```

**السبب:** الكاتب يحتاج صلاحية الكتابة على المحتوى، ولا يجوز أن يملك صلاحية تعديل القوالب أو الميزانيات أو سيور العمل. الفصل يجعل ذلك بنيوياً لا اعتماداً على انضباط.

**ثمن الفصل:** كل نشر يحتاج تنسيقاً بين ثلاثة مستودعات. دفعناه عن قصد.

---

## ٢ · القرارات المعمارية الكبرى

### ٢.١ النشر عبر مستودع الموقع لا FTP — **درس مكلف**

**ما حدث (2026-08-04):** كانت المدونة تُرفع بـFTP إلى `public_html/blog/`. ثم جرى نشر عادي للموقع عبر مزامنة Git في Hostinger — **فمحا مجلد `/blog/` كاملاً**.

**السبب الجذري:** مزامنة Git في Hostinger تنشر محتوى المستودع إلى `public_html` **وتحذف ما ليس فيه**. أي ملف يصل بطريق آخر يُعتبر دخيلاً.

**الحل:** المدونة تُبنى في Actions ثم تُدفع **داخل مستودع الموقع** تحت `blog/`. صارت جزءاً من المصدر لا ضيفاً عليه.

**الثمن ومعالجته:** خطّ المدونة صار يملك صلاحية الكتابة على مستودع الموقع كله. عُوّض بـ`tools/check_deploy_scope.py` — يفشل إن لمس البناء أي مسار خارج `blog/`. اختُبر على ثلاث حالات منها محاولة تعديل `.htaccess`.

### ٢.٢ workflow_dispatch لا repository_dispatch

`repository_dispatch` يتطلب `contents:write` على `blog-build` — أي أن مستودع المحتوى يستطيع تعديل القوالب والميزانيات.

**أُثبت عملياً 2026-08-02:** `repository_dispatch` بتوكن `actions:write` يعطي **403**، و`workflow_dispatch` يعطي **204**. فاعتُمد الثاني.

### ٢.٣ الهيكل يُستخرج من الموقع في كل بناء

**المشكلة:** موقعكم 22 ملف HTML يكرّر الفوتر **نصّاً** في كل واحد. لا مكوّن واحد يولّده. فلا يمكن للمدونة أن «تشارك» ملفاً غير موجود.

**الحل:** `tools/sync_site_chrome.py` يستخرج ثلاث كتل **حرفياً** من صفحة موقع حقيقية:

```
nav      من فتح <main> حتى فتح div.main-wrap    (رصيد +1 div)
navbar   كتلة div.nav-bar بعدّ الوسوم           (رصيد  0)
footer   من div.footer-rounded-div حتى </main>  (رصيد −1 div)
```

و`build.yml` يستنسخ الموقع **قبل** البناء ويشغّل المزامنة. فتغيير رقم هاتف في فوتر الموقع يصل المدونة في النشر التالي **بلا لمسة يد**.

> **قاعدة تُحفَظ:** كتلتا nav و footer تغلّفان المحتوى معاً. اختلال الرصيد = صفحة بوسوم مفتوحة. الأداة تفحص الرصيد وتفشل إن اختلّ.

---

## ٣ · الأعطال وجذورها — **أهمّ قسم في الوثيقة**

### ٣.١ الفوتر بلا تنسيق — التنسيق مكتوب على الأب

**العَرَض:** الفوتر موجود في HTML لكنه يظهر بلا خلفية ولا تنسيق. كل الفحوص النصّية تقول «موجود» وتمرّ.

**الجذر:** قواعد الموقع مكتوبة على **الأب** لا على الفوتر:

```css
.footer-wrap.theme-dark .footer
.footer-wrap.theme-dark .overlay-gradient
```

أخذنا `<footer>` عارياً بلا `footer-wrap theme-dark`، فلم يصله شيء.

**الدرس:** لا تنسخ عنصراً. انسخ **كتلته**. وافحص التنسيق المحسوب لا وجود الوسم.

### ٣.٢ CSS المدونة كان يُعيد تنسيق الموقع

**العَرَض:** الفوتر منزاح 64 بكسل يساراً فيظهر شريط فارغ على الجانب.

**القياس:** `x = −64` في المدونة مقابل `x = 0` في الموقع، ثابتاً عند 1920 و2560.

**الجذر:** `blog.css` كان يحمل قشرة صفحة مستقلة — `html` و`body` و`main` و`*` — وهو يُحمَّل **بعد** `bundle.min.css`. و`body{display:flex}` مع `main{width:100vw}` أنتج الفيضان.

**الحل:** كل قاعدة محصورة تحت `.blog-body`. **لا محدِّد يبدأ بعنصر عارٍ** — مفحوص آلياً.

### ٣.٣ النشر ينجح ولا يُرى — **فخّ immutable**

**العَرَض:** البناء أخضر، الملف على الخادم جديد، `curl` يراه جديداً — **والمتصفح يبقى على القديم**.

**الجذر:** `.htaccess` الموقع يفرض على كل `.css` و`.js`:

```
Cache-Control: public, max-age=31536000, immutable
```

سياسة صحيحة **بشرط** أن يتغيّر اسم الملف مع محتواه. الموقع يفعلها ببصمة `?v=`. لكن `/blog/css/blog.css` كان **رابطاً ثابتاً**.

**كيف كُشف:** قياس CDP أظهر أن المتصفح حمّل `blog.css` بـ86 قاعدة **لا تحوي `.blog-body` إطلاقاً**.

**الحل:** `blog.css` و`fonts.css` عبر خط أصول Hugo مع `fingerprint`:
```
/blog/css/blog.min.a2cccbb8…css
```

> **قاعدة تُحفَظ:** أي CSS/JS جديد يمرّ عبر `assets/` وHugo `fingerprint` — **لا عبر `static/`**. وعند شكوى «التعديل لا يظهر»: افحص **اسم الملف في الرابط** قبل أي شيء آخر.

### ٣.٤ سياق التحويل لا يُورَّث

**العَرَض:** بعد إضافة تحويل www في جذر الموقع، ردّ `www/` بـ301 بينما **ظلّ `www/blog/ar/` يردّ 200**.

**الجذر:** `RewriteEngine On` في `.htaccess` فرعي **يُعيد ضبط سياق mod_rewrite**. قواعد الجذر لا تصل.

**الحل:** القاعدة مكرّرة في `static/.htaccess` و`static/ar/.htaccess` بالبادئة الصحيحة لكلٍّ.

### ٣.٥ اللوحة كتبت تاريخاً يرفضه Hugo

**العَرَض:** البناء يفشل بعد نشر مقال من اللوحة.

**الجذر:** `date="2026-08-07T20:22"` — بلا ثوانٍ ولا منطقة زمنية. وحقل التاريخ في اللوحة كان بلا صيغة محدّدة.

**الحل بثلاث طبقات:**
1. `generate_cms.py` يفرض `format: "YYYY-MM-DDTHH:mm:ssZZ"`
2. `check_content.py` يمسك التاريخ المعطوب **برسالة عربية** قبل Hugo
3. المقالات المكسورة أُصلحت

### ٣.٦ بوابة تصرخ بلا سبب — ثم يمرّ العطل الحقيقي

`check_seo.py` كان يُنذر على ممارستين **صحيحتين**:
- `<title>` داخل `<svg>` — اسم أيقونة لقارئات الشاشة، ليس عنوان صفحة
- `alt=""` — الصواب لصورة زخرفية

**الدرس:** بوابة تُنذر كذباً تُدرَّب على التجاهل، فيمرّ العطل الحقيقي بعدها.

### ٣.٧ المدونة بلا أي تتبّع — ٢٢ صفحة

**كيف كُشف:** سأل المالك «هل أنت متأكد؟». لم أكن. فحصت: الموقع 26/26، **المدونة 0/22**. صفر GTM وصفر GA4 منذ أول نشر.

**الحل:** نفس خاصية GA4 وحاوية GTM — لا خاصية ثانية. فصلها يقطع رحلة الزائر من المقال إلى صفحة الخدمة في التقارير، وهي الرحلة الوحيدة التي تُثبت أن المدونة تبيع.

### ٣.٨ عطلان في الفحص نفسه — لا في الموقع

| الادعاء | الحقيقة |
|---|---|
| «20 صفحة يتيمة» | نمطي طلب `href="` باقتباس، ومصغّر Hugo يكتب `href=https://…` بلا اقتباس. **الرقم الصحيح: 2** وكلاهما 404 مقصودة |
| «31 صفحة رقيقة» | عددتُ الناف بار والفوتر محتوى — لأنهما داخل `<main>` في المدونة. **الرقم الصحيح: صفر** |
| «الموقع ساقط» | Google وGitHub يفتحان، ونطاقكم وحده يتجاوز المهلة. **جدار Hostinger يحجب عنوان خروجي** — أثبته خادم GitHub بالوصول بنجاح |

> **قاعدة تُحفَظ:** صحّح أداة القياس على حالة معروفة النتيجة **قبل** تصديقها على الحالة المجهولة.

---

## ٤ · طبقة الضوابط — ١٧ فاحصاً

| الأداة | ما تمنعه |
|---|---|
| `check_controls.py` | تعديل أي ملف ضوابط بلا تسجيل بصمته (42 ملفاً) |
| `check_deploy_scope.py` | لمس أي ملف موقع خارج `blog/` |
| `check_content.py` | نشر مقال ناقص أو بتاريخ معطوب |
| `check_seo.py` | عنوان/وصف/canonical/hreflang ناقص |
| `check_indexing.py` | خرائط أو RSS أو IndexNow معطوبة |
| `check_budgets.py` | تآكل الأداء |
| `check_cms.py` | انحراف اللوحة عن المخطّط |
| `sync_site_chrome.py --check` | انحراف الهيكل عن الموقع |

**البصمة الذاتية:** `check_controls.py` يبصم نفسه. من يعطّل بوابة يفعلها في تغيير ظاهر لا صامت.

---

## ٥ · الميزانيات — ولماذا قياس النقل لا القرص

```
CSS كامل        26 KB    الخطوط  110 KB لكل لغة
أكبر صورة      500 KB    المسار الحرج  220 KB
```

**القياس: brotli للنصوص، وحجم فعلي للصور والخطوط** (WOFF2 وWebP مضغوطان أصلاً).

**درس من الموقع الرئيسي (2026-07):** قياس CSS خاماً أعطى 120 KB وقاد إلى **توصية خاطئة** بتقسيمه. القياس المضغوط الحقيقي كان **17 KB**، والتقسيم كان سيضرّ.

> **قِس ما يعبر السلك، لا ما على القرص.**

**قرار مالك موثّق:** حدّ CSS رُفع 20 → 26 KB حين تبنّت المدونة هيكل الموقع. مكتوب في `budgets.json` **مع سببه** — ولا يُرفع ثانيةً بلا قرار مثله.

---

## ٦ · خطّ النشر — 24 خطوة

```
قوالب ← محتوى ← بصمات الضوابط ← فحص اللوحة ← تجهيز المحتوى ← تدقيق
   ↓
مستودع الموقع ← مزامنة الهيكل ← Hugo (ببصمة) ← بناء
   ↓
فهرسة ← SEO ← ميزانيات ← استبدال blog/ ← حارس النطاق ← دفع
   ↓
تحقّق خارجي ← IndexNow ← مراقبة ← ملخّص
```

**الترتيب مقصود:** الموقع يُستنسخ **قبل** البناء لأنه مصدر الهيكل لا وجهة النشر وحدها.

### تأخّر المزامنة تحذير لا فشل

الدفع يتمّ **قبل** خطوة التحقّق. فحين تتأخّر Hostinger، النشر نجح والتشغيل يظهر أحمر.

**إشارة تكذب أخطر من غياب الإشارة** — من يرى أحمر على نشر ناجح مرّتين يتوقّف عن قراءة اللون.

النافذة 600 ثانية، و`continue-on-error`، وIndexNow مشروط بنجاح التحقّق، والملخّص يشرح بالعربية.

---

## ٧ · قرارات رُفضت وسبب رفضها

| البديل | لماذا رُفض |
|---|---|
| `repository_dispatch` | يتطلب `contents:write` — أُثبت 403 |
| رفع FTP | مزامنة Hostinger تمحو ما ليس في المستودع |
| إعادة كتابة الهيكل بأسلوبنا | يُنتج ما **يشبه** الموقع ولا يطابقه، وينحرف مع كل تعديل |
| خاصية GA4 منفصلة | تقطع رحلة الزائر من المقال إلى الخدمة |
| Skeleton Loaders | موقع HTML ثابت بلا جلب بيانات — مسرح يبطئ الصفحة |
| استبعاد noindex من الخريطة | يتصادم مع عقد hreflang — **تُرك مفتوحاً بصراحة** |
| الخطّ المتغيّر (variable) | 62.8 KB مقابل 56.4 لوزنين ثابتين |

---

## ٨ · ما يجب ألّا يُكرَّر

1. **لا تصدّق أداة قياس قبل تصحيحها على حالة معروفة.** `/dev/tcp` أعطى فشلاً على منفذ سليم وأنتج تشخيصاً وهمياً أُرسل للدعم.
2. **`if curl … | tail` يقيس `tail` لا `curl`.** أُعلن نجاح رفع مرّتين وقد فشل.
3. **`dict(r.headers)` يفقد عدم حساسية الحالة.** HTTP/2 يرسل `location` صغيراً، فبدت 8 تحويلات سليمة معطوبة.
4. **‏TOML: `disableKinds` بعد `[permalinks]`** يصير `permalinks.disableKinds` — فشل صامت.
5. **`cd X` بلا `|| exit`** — نفّذ أوامر git في المستودع الخطأ.
6. **لقطة الشاشة تُثبت أن الشيء يُرسَم، لا أنه في موضعه.** قسم استقرّ **بعد `</body>`** والمتصفح رفعه، فبدت اللقطة سليمة.
7. **لا تدفع مستودعين في اللحظة نفسها** — البناء يلتقط القالب القديم.
8. **افحص قبل الدفع لا بعده.** دفعتُ ثم قرأت نتيجة البوابة فسقط البناء.

---

## ٩ · لبناء مدونة ثانية — الترتيب

1. **الضوابط أولاً**: `schema.json` و`budgets.json` و`fingerprints.txt` — قبل سطر HTML واحد
2. **آلية التشغيل**: `workflow_dispatch` + توكن دقيق الصلاحية
3. **الخطوط مستضافة ذاتياً** — قِس الأوزان المستعملة فعلاً بـ`getComputedStyle`، لا تفترضها
4. **طبقة SEO على صفحة منشورة فعلياً** — لا على قالب فارغ
5. **الهيكل بالاستخراج الآلي** من الموقع، لا بإعادة الكتابة
6. **النشر داخل مستودع الموقع** + حارس نطاق
7. **البوابات قبل النشر** — وكل بوابة تُختبر على حالة تفشل وحالة تنجح
8. **التتبّع من اليوم الأول** — لا تكتشف بعد شهر أنه غائب

---

## ١٠ · ما بقي مفتوحاً

| البند | الحالة |
|---|---|
| ترجمة مقالين إلى الإنجليزية | مقال واحد من ثلاثة مترجَم |
| صور دراسات الحالة | نصّها إنجليزي على الصفحات العربية |
| `noindex` داخل الخريطة | 8 صفحات — يحتاج قراراً في عقد hreflang |
| أيقونة موقع للمدونة | 22 صفحة بلا `rel=icon` |
| 11 صورة PNG ضخمة | ~12 MB بلا مرجع في أي ملف |
| رفع HSTS إلى سنة | بعد شهر استقرار — **~9 سبتمبر** |
| رموز 2FA · FTP الميت · التوكنات · DMARC | **بيد المالك — لا تُفوَّض** |

---

## ملحق · أسماء يجب معرفتها

**الأسرار:** `SITE_REPO_TOKEN` · `CODEBERG_TOKEN` · `PREVIEW_FTP_*` · `HEALTHCHECKS_URL`

**الوثائق داخل `blog-build`:** `CLAUDE.md` (القواعد) · `ARCHITECTURE.md` (البنية) · `HANDOFF.md` (التسليم) · `WRITER_GUIDE_AR.md` (دليل الكاتب)

**نقطة حرجة:** `blog-content` يستبدل `content/` كاملاً في كل بناء. صفحات تعريف التصنيفات في `taxonomy-content/` داخل `blog-build` كي لا تضيع.
