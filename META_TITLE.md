# كيف أضفنا حقل «عنوان محركات البحث»

وثيقة عمليّة لحقل `meta_title`: ما المشكلة، ولماذا صُمّم هكذا، وأي ملفات
لُمست وبأي ترتيب، وما الذي كسر النشر في الطريق. وفي آخرها **وصفة عامّة**
لإضافة أي حقل جديد إلى اللوحة — فلا يُعاد استنتاج هذا مرّة ثانية.

الإصدار: `4b0a1cf` (blog-build) · 2026-09-01

---

## ٠ · الخلاصة في دقيقة

حقل `title` كان يؤدّي **ثلاث وظائف معاً**: العنوان `h1` داخل الصفحة،
و`<title>` في نتائج جوجل، وعنوان بطاقة المشاركة. فمن أراد عنواناً
مُحسَّناً للفهرسة اضطُرّ لتغيير العنوان الذي يقرؤه الزائر.

أضفنا حقلاً **اختيارياً** اسمه `meta_title`. فارغاً: لا شيء يتغيّر حرفاً
بحرف. مملوءاً: يحلّ محلّ `<title>` **كما كُتب** — بلا لاحقة اسم المدونة،
وبلا أن يمسّ `h1` أو `og:title`.

خمسة ملفات، ترتيبها إلزاميّ، وثلاث بوّابات تفرض اتّساقها.

---

## ١ · لماذا الحقل اختياري لا إلزامي

لأن الإلزام يفرض على الكاتب صياغة عنوانين لكل مقال، وأغلب المقالات
عنوانها التحريري صالح للفهرسة كما هو. والاختياري يعني أن **السلوك القديم
هو الافتراضي**، فلا يتغيّر شيء في ٩٩٪ من المقالات، ولا تحتاج المقالات
المنشورة إلى أي تعديل بأثر رجعي.

قِسنا هذا ولم نفترضه: بنينا الموقع قبل التغيير وبعده، فخرج `<title>` لمقال
`build-marketing-strategy` متطابقاً بالضبط ما دام الحقل فارغاً.

---

## ٢ · القرارات الأربعة وتبريرها

### ٢.١ يُستعمل حرفياً بلا لاحقة اسم المدونة

السلوك القديم يبني `«العنوان — من الصفر إلى الواحد»` ما دام المجموع ≤٧٠
محرفاً. لكن من يصوغ عنواناً لجوجل عمداً يريد **ما كتبه** لا ما نضيفه إليه؛
فاللاحقة تسرق منه ٢٠ محرفاً من أصل ٧٠ بلا إذنه.

فالقاعدة صريحة: `meta_title` موجود ⟵ هو `<title>` كاملاً. غائب ⟵ السلوك
القديم بلاحقته.

### ٢.٢ لا يمسّ `og:title` ولا `twitter:title`

بطاقة المشاركة يقرؤها **إنسان** على واتساب أو لينكدإن، لا زاحف. والعنوان
التحريري أنسب لها من عنوان مُحسَّن لكلمات مفتاحية. فبقيت البطاقتان على
`.Title`.

هذا خيار مقصود لا سهو. إن أردت لاحقاً أن يسري على البطاقة أيضاً، غيّر
`$pageTitle` في السطرين ٧٦ و٨٩ من `layouts/partials/head.html` — لكن
اعلم أنك تفقد التمييز.

### ٢.٣ لا يمسّ `h1`

`h1` هو ما يقرؤه الزائر بعد أن ينقر. تغييره يعني أن يرى في الصفحة عنواناً
غير الذي نقر عليه في جوجل — وهو ما تُسمّيه محرّكات البحث «طُعم ومبادلة»،
ويضرّ أكثر ممّا ينفع.

### ٢.٤ الحدّان ١٠–٧٠ ليسا اعتباطاً

`tools/check_seo.py` يفرض على عنصر `<title>` الناتج:

```python
check.require(page, 10 <= len(title) <= 70,
              f"طول title خارج 10–70 محرفاً ({len(title)})")
```

فلو سمحنا في اللوحة بمدى أوسع، لكتب الكاتب عنواناً مقبولاً في اللوحة
**ثم سقط البناء بعد الحفظ** — وهو أسوأ أنواع الأعطال: بعيد عن سببه.
فجعلنا حدّي العقد يطابقان حدّي البوّابة تماماً، ليُمسَك الخطأ في اللوحة
لحظة الكتابة.

---

## ٣ · الملفات الخمسة — والترتيب إلزاميّ

| # | الملفّ | ما يُضاف |
|---|---|---|
| ١ | `controls/schema.json` | تعريف الحقل: النوع، الاختيارية، الحدّان، رسالة الخطأ |
| ٢ | `tools/generate_cms.py` | `LABELS` + `HINTS` + إدراجه في `build_fields` |
| ٣ | `layouts/partials/head.html` | استعماله في بناء `<title>` |
| ٤ | `preview/admin/config.yml` | **مُولَّد** — لا يُحرَّر بيد |
| ٥ | `WRITER_GUIDE_AR.md` | **مُولَّد** — لا يُحرَّر بيد |

الرابع والخامس يخرجان من أمر واحد:

```bash
python3 tools/generate_cms.py
```

**ولهذا الترتيب إلزاميّ**: العقد أولاً، فالمولّد يقرأ منه، فالتوليد يكتب
الملفّين. من يحرّر `config.yml` بيده يخسر تعديله عند أول توليد.

### ما دخل العقد

```json
"meta_title": {
  "type": "string",
  "required": false,
  "min_length": 10,
  "max_length": 70,
  "error_ar": "عنوان محركات البحث يجب أن يكون بين 10 و70 محرفاً. عنوانك الآن {actual} محرفاً."
}
```

`{actual}` نائب يستبدله المدقّق بالعدد الحقيقي، فيرى الكاتب «عنوانك الآن
٧١ محرفاً» لا رسالة عامّة.

### ما دخل القالب

```gotemplate
{{- $metaTitle := .Params.meta_title | default "" -}}
{{- $documentTitle := $pageTitle -}}
{{- if $metaTitle -}}
  {{- $documentTitle = $metaTitle -}}
{{- else if not .IsHome -}}
  {{- $brandedTitle := printf "%s — %s" $pageTitle .Site.Title -}}
  {{- if le (strings.RuneCount $brandedTitle) 70 }}
    {{- $documentTitle = $brandedTitle -}}
  {{- end -}}
{{- end -}}
```

`else if` لا `if` منفصلة: الفرعان يتنافيان، فمن وضع عنواناً صريحاً لا
تُضاف إليه اللاحقة أبداً.

---

## ٤ · تشابك البوّابات — ولماذا لا يمكن نسيان خطوة

ثلاث بوّابات تتقاطع على هذا التغيير، وكلٌّ منها تمسك نسياناً مختلفاً:

**`tools/check_cms.py`** — يفرض تطابق حقول اللوحة مع العقد حرفاً بحرف:

```python
expected_names = set(schema["fields"]) | {"body"}
if set(fields) != expected_names:
    problems.append("حقول المقال لا تطابق schema.json + body")
```

فمن أضاف الحقل إلى العقد ونسي `build_fields` — **يسقط بناؤه**. لا يمكن
أن ينحرف الاثنان بصمت.

**`tools/check_content.py`** — يمرّ على حقول العقد عموماً:

```python
for name, rule in fields.items():
    ...
    if rule["type"] == "string":
        n = visible_length(val)
        lo, hi = rule.get("min_length"), rule.get("max_length")
```

أي أن **التحقّق يأتي مجاناً**: لا سطر واحد كُتب فيه للحقل الجديد. وهذا
مقصود في تصميم المدقّق — الحقول بيانات لا شيفرة.

**`tools/check_controls.py`** — يبصم ٤٤ ملفاً (كانت ٤٣ قبل أن تُضاف هذه
الوثيقة نفسها إلى الحراسة). غيّرنا خمسة منها وقت التغيير، فاشتكى
بخمس مشاكل. والتحديث فعل متعمّد:

```bash
python3 tools/check_controls.py --update
```

⚠️ **افحص ما بارَكتَه قبل التحديث.** `--update` يعيد بصم كل شيء، فلو كان
هناك انحراف سابق لملفّ لم تلمسه، سيُبارَك بصمت. تحقّقنا: خمسة أسطر
بصمات تغيّرت، وهي بالضبط ملفّاتنا الخمسة، لا سادس.

---

## ٥ · ما اختُبر — والقياس قبل الادّعاء

| الاختبار | النتيجة |
|---|---|
| بناء قبل التغيير | `<title>` = «كيف تبني استراتيجية تسويق ناجحة لشركتك من الصفر؟» |
| بناء بعده والحقل **فارغ** | مطابق حرفاً بحرف — لا انحدار |
| الحقل مملوء | `<title>` صار العنوان الجديد (٤٨ محرفاً) |
| و`og:title` | لم يتغيّر ✓ |
| و`h1` | لم يتغيّر ✓ |
| حدّ أدنى: ٤ محارف | فشل برسالة تذكر «٤» ✓ |
| حدّ أعلى: ٧١ محرفاً | فشل برسالة تذكر «٧١» ✓ |
| `check_content` | ٧ ملفات بلا مشاكل |
| `check_cms` | سليم |
| `check_seo` | ٢٠ صفحة، hreflang متبادل |
| `check_controls` | ٤٣ ملفاً مطابقاً |

اختبار الحدّين ليس ترفاً: هو ما يثبت أن العقد **يمنع** لا أنه يزيّن.
بلا اختبار فاشل مقصود، لا تعرف أن الحارس يعمل.

---

## ٦ · النشر — لا يحدث تلقائياً

الدفع إلى `main` **لا ينشر شيئاً**. سيرا العمل كلاهما `workflow_dispatch`
فقط:

```bash
gh workflow run preview.yml --repo zeroone01z21-alt/blog-build   # لوحة الكاتب
gh workflow run build.yml   --repo zeroone01z21-alt/blog-build   # المدونة الحيّة
```

`preview` هو ما يُظهر الحقل الجديد للكاتب. `build` يعيد بناء المدونة
ويسلّمها داخل مستودع الموقع (لا FTP — راجع `BLOG_PLAYBOOK.md` ٢.١).

### فخّ الاعتماد

`git push` هنا **لا يقرأ سلسلة مفاتيح macOS**:

```
credential.https://github.com.helper = !gh auth git-credential
```

أي أنه يستعمل **الحساب النشط في `gh`**. والمستودعات يملكها
`zeroone01z21-alt` بينما الحساب النشط عادةً `01team9639-maker` وهو بلا
صلاحية (`push=false`). فرسالة الرفض `403` تبدو نقص صلاحية بينما هي نقص
**تبديل**:

```bash
gh auth switch --user zeroone01z21-alt   # افعل
git push origin main
gh auth switch --user 01team9639-maker   # ثم أعده كما كان
```

---

## ٧ · ثلاثة أعطال ظهرت أثناء النشر — لا علاقة لها بالحقل

توثَّق لأنها ستتكرّر:

**٧.١ النسخة المحلّية كانت متأخّرة ١٨ إصداراً.** لو عدّلت قبل التحديث
لبنيتُ على شجرة قديمة. **افحص `git rev-list --count HEAD..origin/main`
قبل أي تعديل** في المستودعات الثلاثة.

**٧.٢ مقال مؤرشف كان يحجب كل نشر.** `why-ad-campaigns-fail` — مسودة
ومؤرشف، أي غير منشور — كان فيه تاريخ `2026-08-14T18:25` بلا ثوانٍ ولا
منطقة زمنية، وعناوين كلها من المستوى ٣ فأوّلها قفزة من ١ إلى ٣.
و`check_content` يفحص **كل** المحتوى قبل أن يستبعد `prepare_content.py`
المؤرشف. فمقال لا يراه أحد أوقف اللوحة والموقع معاً.

> عطل التاريخ نفسه موثّق في `BLOG_PLAYBOOK.md` ٣.٥ — تكرّر لأن اللوحة
> تكتب `datetime-local` بلا ثوانٍ. الصيغة الصحيحة `+03:00`.

**٧.٣ النسخ الاحتياطي مكسور منذ خمسة أسابيع بصمت.** `git push --mirror`
يدفع `refs/*` كاملة ومنها `refs/pull/*` التي يصنعها GitHub لكل طلب سحب،
وGitea على Codeberg يرفضها: `deny updating a hidden ref`. فانكسر النسخ
عند أول طلب سحب (٢٠٢٦-٠٨-٠٩) وفشل خمس مرّات متتالية — **وأخفاه أن
`blog-deploy` بقي أخضر** لأنه بلا طلبات سحب، فبدا التقرير «واحدة من
ثلاث» لا فشلاً تامّاً. العلاج: دفع صريح للفروع والوسوم مع `--prune`.

---

## ٨ · الوصفة العامّة — لأي حقل جديد

```bash
# ١) عرّفه في العقد
$EDITOR controls/schema.json          # اسم، نوع، required، الحدود، error_ar

# ٢) سمّه واشرحه وأدرجه في اللوحة
$EDITOR tools/generate_cms.py         # LABELS + HINTS + build_fields

# ٣) استعمله في القالب
$EDITOR layouts/partials/head.html    # أو أي قالب آخر

# ٤) ولّد اللوحة والدليل
python3 tools/generate_cms.py

# ٥) تحقّق قبل البصم
python3 tools/check_content.py content
python3 tools/check_cms.py

# ٦) اختبره فعلياً — لا تكتفِ بالبوّابات
hugo --gc --minify --destination /tmp/base    # قبل
#   ثم أضف قيمة للحقل في مقال وأعد البناء وقارن
python3 tools/check_seo.py /tmp/after

# ٧) اختبر الحدّين بقيمة فاشلة مقصودة

# ٨) ابصم — بعد فحص ما ستباركه
python3 tools/check_controls.py --update
git diff --numstat -- controls/fingerprints.txt   # هل العدد = ملفّاتك؟

# ٩) ادفع ثم انشر (لا شيء تلقائي)
gh auth switch --user zeroone01z21-alt
git push origin main
gh workflow run preview.yml --repo zeroone01z21-alt/blog-build
gh auth switch --user 01team9639-maker
```

**قاعدة الحدود:** إن كان الحقل يغذّي عنصراً تفحصه `check_seo.py`، انسخ
حدود البوّابة إلى العقد حرفياً. وإلا انتقل الخطأ من اللوحة إلى البناء.

---

## ٩ · ما بقي مفتوحاً

- **النسخة العربية من `why-ad-campaigns-fail` تستعمل `**نصّاً عريضاً**`
  بدل العناوين** في المواضع الثمانية نفسها التي فيها الإنجليزية عناوين
  حقيقية. لا يوقف البناء — ولهذا لم يظهر في التدقيق — لكنه يُلغي بنية
  العناوين للزاحف تماماً.
- **«Men Al-Sifr Lil-Wahid Company»** في عنوان إنجليزي: نقل حرفيّ
  لـ«من الصفر إلى الواحد». الاسم الإنجليزي «Zero to One».
- **`meta_title` لا يظهر في RSS.** `layouts/_default/rss.xml` يستعمل
  `.Title`، وهو الصحيح — قارئ الخلاصة إنسان لا زاحف — لكنه موثَّق هنا
  لئلّا يُقرأ سهواً.
