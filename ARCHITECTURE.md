# البنية — مدونة ZERO 2 ONE

> **العقد المكتوب بين المستودعات الثلاثة.** القسم 16 من سجل القرارات.
>
> السبب: قوالب البناء تتوقع حقولاً معيّنة. تغييرٌ في طرف يكسر كل بناء في
> الطرف الآخر، والكاتب لا يعرف لماذا. **ما لا يُعرَّف مكتوباً لا يُحترم.**

آخر تحديث: 2026-08-02 · الحالة: **المرحلة 1 قيد التنفيذ** — لا قوالب بعد.

---

## 1. الخريطة

```
   الكاتب
     │  يحفظ في اللوحة
     ▼
┌─────────────────┐   إشارة repository_dispatch    ┌──────────────────┐
│  blog-content   │ ──────────────────────────────▶│    blog-build    │
│                 │   (رمز دقيق: actions:write فقط)│                  │
│ مقالات + صور    │◀────────── سحب بقراءة فقط ─────│ قوالب · إعدادات  │
│ Markdown        │                                 │ سير عمل · ميزانيات│
└─────────────────┘                                 └────────┬─────────┘
   الكاتب: Write                                              │ عند النجاح
   المالك: Admin                                              ▼
                                                    ┌──────────────────┐
                                                    │   blog-deploy    │
                                                    │  HTML مبني فقط   │
                                                    │  Actions معطّلة  │
                                                    └────────┬─────────┘
                                                             │ SFTP مقيَّد
                                                             ▼
                                                   zero2one.sa/blog/
```

**لماذا الفصل ثلاثي:** مستودع المحتوى لا يحوي منطق بناء ولا أسراراً ذات قيمة.
اختراق حساب الكاتب لا يمنح المهاجم خط بناء يعدّله. أقصى ضرر: **مقال سيّئ +
تشغيل بناء عبثي** — كلاهما قابل للتراجع بدقيقة.

---

## 2. الحسابات والمستودعات

| المستودع | الحساب | الرؤية | Actions |
|---|---|---|---|
| `blog-content` | `zeroone01z21-alt` | عام | سير عمل واحد: إشارة فقط |
| `blog-build` | `zeroone01z21-alt` | عام | كامل |
| `blog-deploy` | `zeroone01z21-alt` | عام | **معطّلة** |

مستودع الموقع `01team9639-maker/zero2one-web` **منفصل تماماً** ولا يُمَس.

### لماذا «عام»

قرار واعٍ من المالك (2026-08-02) بعد مناقشة المخاطر: المحتوى مقالات بلا بيانات
عملاء ولا أرقام نتائج. نُوقشت مخاطر كشف المسودات وديمومة تاريخ Git وقُبلت.

**نتيجتان لم تكونا في الحسبان، وكلتاهما في صالحنا:**

1. **حماية الفروع و CODEOWNERS مجانية** على المستودعات العامة. على الخاصة
   تتطلب باقة مدفوعة. أي أن القسم 12.6 صار **قابلاً للتنفيذ حرفياً بلا اشتراك** —
   وقد بدا مستحيلاً قبل ذلك.
2. **دقائق Actions بلا حدود** (الخاصة: 2000/شهر). يسقط حساب السعة في القسم 6.4
   بالكامل. الوتيرة المستهدفة «مقال يومياً» لا تصطدم بشيء.

> ⚠️ **تحذير للمستقبل:** تحويل `blog-build` إلى خاص **يُسقط حماية الفرع صامتةً**
> على الخطة المجانية. لا تغيّر الرؤية قبل قراءة هذه الفقرة.

**شرط إعادة النظر:** إن بدأ المحتوى يضم دراسات حالة بأسماء عملاء أو أرقام
نتائج → `blog-content` يصير خاصاً فوراً، ويُعاد تقييم بدائل الحماية.

---

## 3. عقد الحقول

**المصدر الواحد: [`controls/schema.json`](controls/schema.json).**
يقرأ منه سكربت التحقق قبل البناء، وإعداد لوحة Sveltia، وهذا الملف.
**لا تُكرَّر القيم هنا** — التكرار يُنتج انحرافاً.

خلاصة غير مُلزِمة (المُلزِم هو JSON):

| الحقل | إلزامي | القيد |
|---|---|---|
| `title` | ✅ | 10–70 محرفاً |
| `description` | ✅ | 50–160 محرفاً |
| `slug` | ✅ | حروف صغيرة وأرقام وشرطات · **يُقفل بعد أول نشر** |
| `date` · `lastmod` | ✅ / ❌ | تلقائيان |
| `draft` | ✅ | افتراضي `true` |
| `archived` | ❌ | افتراضي `false` |
| `categories` | ✅ | واحد على الأقل |
| `featured_image` | ✅ | داخل حزمة الصفحة |
| `featured_image_alt` | ✅ | غير فارغ |

**رسائل الخطأ بالعربية** — الكاتب غير تقني ويقرؤها هو، لا المطوّر.

### بنية الحزمة

```
content/blog/<slug>/
    index.en.md
    index.ar.md          ← الترجمة بامتداد اللغة داخل نفس الحزمة
    cover.webp
    *.webp
```

الصور **داخل حزمة المقال حصراً**، لا مجلد صور عام. يُلغي هذا مشكلة «الصورة غير
المستخدمة» تماماً: حُذف المقال، حُذف مجلده معه. **لا حذف آلي، ولا إيجابي كاذب
يمحو صورة حية.**

---

## 4. الروابط واللغة

| | الإنجليزية | العربية |
|---|---|---|
| الموقع | `/about/` | `/ar/about/` |
| المدونة | `/blog/{slug}/` | `/blog/ar/{slug}/` |

**القاعدة واحدة في الموضعين: الإنجليزية بلا بادئة، العربية ببادئة.**

`x-default` → الإنجليزية. `hreflang` متبادل. `lang` و `dir` تلقائيان.

**قرار بند صفر (2026-08-01):** نُقض قرار «العربية افتراضية» في النسخ 1.0–2.2
لأنه بُني على وصف خاطئ للموقع. الفحص أثبت `<html lang="en">` في الجذر و
`x-default` يشير إلى `https://zero2one.sa/`.

المبرَّر: **بادئة المسار أثرها على الترتيب صفر** — أقرّه السجل نفسه في القسم 8.1.
فالمعيار الحاسم اتساق التجربة. ولو كان `/` إنجليزياً و`/blog/` عربياً لأصبح
المنطق معكوساً داخل النطاق الواحد.

**بلا تاريخ وبلا تصنيف في المسار** — التاريخ يجعل المقال يبدو قديماً، والتصنيف
يكسر الرابط عند إعادة التصنيف. **الرابط بعد النشر لا يتغيّر.**

---

## 5. النشر

**SFTP بحساب مقيَّد على `/blog/` حصراً.**

### لماذا سقط ربط Git الأصلي

توثيق Hostinger الرسمي (2026-08-01): الميزة تدعم **مستودعاً واحداً لكل موقع**.
الخيار المتاح *Change repository*، وتحذيره حرفياً: «ملفات موقعك السابقة ستُستبدَل
بالمستودع الجديد».

وموقع zero2one.sa **مربوط أصلاً** على `main` → `public_html`. ربط `blog-deploy`
يعني **مسح الموقع بالكامل**.

> 🚫 **لا تُفتح شاشة hPanel ← Advanced ← Git إطلاقاً.**

### أثر على دور blog-deploy

لم يعد مصدر السحب للاستضافة. دوره الآن **مخزن نسخ للتراجع** — سجل ثابت لِما هو
حيّ بالضبط، ونقطة استعادة بخطوة واحدة. يبقى قائماً لهذا السبب وحده.

### البديل المرفوض

دفع المخرجات إلى `/blog/` داخل مستودع الموقع: يمنح خط بناء المدونة **صلاحية
كتابة على مستودع الموقع الحي**. GitHub لا يقيّد الرمز بمسار، فدفعة خاطئة تكسر
الموقع كله. **يناقض مبدأ العزل.**

---

## 6. الأمان

### الضوابط الفعّالة

| الطبقة | الآلية | ما تمنعه |
|---|---|---|
| فصل الصلاحيات | الكاتب لا يملك وصولاً لـ`blog-build` | لا يستطيع لمس سير العمل ولا الميزانيات — **لا لأن قاعدة تمنعه، بل لأنه لا يرى المستودع** |
| CODEOWNERS + حماية الفرع | مجانية (مستودع عام) | تعديل الضوابط بلا مراجعة |
| بصمة الضوابط | [`tools/check_controls.py`](tools/check_controls.py) | رفع حدّ ميزانية صامتاً |
| Actions معطّلة على `blog-deploy` | إعداد المستودع | أي تنفيذ في مستودع النشر |
| رمز الإشارة | دقيق الصلاحية: `actions:write` على `blog-build` فقط | **عديم القيمة إن سُرِّب** — أقصى ما يفعله المهاجم تشغيل بناء |
| رمز السحب | قراءة فقط | كتابة في المحتوى |

### الصراحة الواجبة عن البصمة

**تلاعب مكشوف، لا تلاعب ممنوع.** من يملك الكتابة يستطيع تعديل الملف وبصمته معاً.
الفرق أن ذلك يصير فعلاً متعمّداً ظاهراً في الـdiff. **حماية الفرع هي ما يحوّله
إلى ممنوع** — وهي مفعّلة هنا مجاناً بفضل الرؤية العامة.

### المصادقة

Sveltia **لا تملك نظام مستخدمين**. تعتمد على GitHub للهوية والصلاحيات — لذلك
**الكاتب يحتاج حساب GitHub**: الحفظ يكتب في المستودع باسمه. ميزة جانبية: كل
تغيير منسوب لصاحبه وقابل للتراجع، ولا كلمات مرور نخزّنها.

**تدفّق OAuth عبر وسيط**، لا «الدخول برمز» — لأن الرمز الشخصي **ينتهي كل 90
يوماً** ويحتاج توليداً يدوياً، وهذا لا يصلح لكاتب غير تقني.

الوسيط: `sveltia-cms-auth` على Cloudflare Worker (`*.workers.dev`).
**لا علاقة له بنطاقنا ولا بأي سجل DNS.**

---

## 7. مخالفات واعية للسجل

مسجّلة صراحةً حتى لا تُقرأ لاحقاً كسهو:

| القسم | المخالفة | المبرَّر |
|---|---|---|
| 5 | المستودعات **عامة** لا خاصة | قرار المالك بعد مناقشة المخاطر. فتحت الحماية المجانية |
| 17 | **لا نقل للنطاق** إلى Cloudflare | تغيير خوادم الأسماء أخطر عملية في الخطة، وغير ضروري للإطلاق. القسم 17 نفسه يذكر بديل ترويسات الاستضافة |
| 15.1 | التراجع **خطوتان** لا ثلاث | لا طبقة حافة ⇒ لا تفريغ ذاكرة. الأثر محدود لأن تخزين Hostinger أقصر عمراً |
| 18 | GA4 **بلا بوابة موافقة** | بانر على المدونة وحدها = صفحة من كل 21. يزعج القارئ ولا يحقق امتثالاً لأن باقي الموقع بلا موافقة. **PDPL فجوة مفتوحة على مستوى الموقع كله** — مشروع منفصل |

---

## 8. افتراضات ثبت خطؤها

**لا تُقترح ثانية:**

| الافتراض | الواقع |
|---|---|
| Sveltia تدعم فروع المسودات (Editorial Workflow) | **لا تدعمها.** تعمل على فرع واحد. النسخة 1.0 بُنيت على ميزة غير موجودة |
| العربية افتراضية على الموقع | **الإنجليزية في الجذر.** أثبته فحص `lang` و`x-default` |
| Hostinger تقبل مستودعين لنفس الموقع | **مستودع واحد.** الثاني يستبدل الأول |
| بناء واحد لكل نشر | **كل حفظة تشغّل بناءً.** 30 حفظة = 30 بناءً بلا إلغاء متزامن |
| «التراجع بدقيقة» | خاطئ — الحالة تستمر بتقديم النسخة الفاسدة |
| تضمين CSS الحرج يحسّن الأداء | **جُرّب على الموقع الرئيسي: النتيجة 71 ← 41، و CLS 0.045 ← 1.12.** أُلغي |
| قياس CSS خاماً مؤشر مفيد | خاماً 120 كيلوبايت، مضغوطاً **17**. القياس الخام قاد لتوصية خاطئة. **قِس ما يعبر السلك** |

---

## 9. إجراءات الطوارئ

### التراجع عن نشر سيّئ — خطوتان

1. استعادة النسخة السابقة من `blog-deploy` ورفعها عبر SFTP
2. **تحقق خارجي** أن الصفحة الحية تعرض النسخة السليمة

*(الخطوة الثالثة — تفريغ ذاكرة الحافة — ساقطة لأننا لا نستخدم Cloudflare للنطاق)*

### فقدان الكاتب للوصول

رموز استرداد GitHub تُولَّد عند الإعداد وتُحفظ في مدير كلمات مرور المالك.
طريقتا مصادقة مسجَّلتان على حساب الكاتب. مراجعة نصف سنوية.

### تجاوز البوابات

**من حساب المالك حصرياً · بسبب مكتوب · لعملية نشر واحدة · مراجعة خلال 48 ساعة.**
**لا يشمل الأمان ولا التعقيم — هذان لا يُتجاوزان إطلاقاً.**

---

## 10. الصيانة

| الدورية | المهمة |
|---|---|
| أسبوعي | مراجعة تنبيهات المراقب · التحقق من نجاح المرآة على Codeberg |
| شهري | حجم المستودع · تنبيه المسودات القديمة (**بلا حذف**) · مراجعة Search Console |
| ربع سنوي | تحديث Hugo و Sveltia (نسخ مثبَّتة) · مراجعة بصمات الإجراءات · فحص الروابط المكسورة |
| نصف سنوي | **اختبار استرداد نسخة احتياطية فعلياً** · التحقق من رموز استرداد الكاتب |
| سنوي | مراجعة معيارية: هل القرارات ما زالت صحيحة؟ |

**النسخ الاحتياطي:** مرآة أسبوعية على **Codeberg** — مؤسسة غير ربحية في ألمانيا،
أي شركة وقوانين وبنية مختلفة عن GitHub/مايكروسوفت. الغرض ألا تسقط الاثنتان في
نفس الحدث. + نسخة محلية شهرية وقرص خارجي ربع سنوي.

---

## 11. معلّق

- [ ] `/blog/` فارغ على الاستضافة · حساب SFTP مقيَّد عليه
- [ ] نطاق `preview` للوحة والمسودات
- [ ] الكاتب: الاسم وحساب GitHub ورموز الاسترداد
- [ ] وصول DNS لتحقق Search Console
- [ ] حساب Cloudflare مجاني (Worker فقط — المرحلة 4)
- [ ] حذف المستودعات القديمة على `zero2one012025-png`
- [ ] **المقال الأول:** منشور حالياً على `zero2onemarketing.blogspot.com`.
      نشره عندنا دون معالجة = **الموقع ينافس نفسه**. يجب حذفه من blogspot أو
      وضع `rel="canonical"` فيه يشير إلينا **قبل** النشر
- [ ] الأرقام المقيسة: زمن البناء · عدد البناءات بعد الإلغاء · زمن التراجع ·
      أحجام الخطوط — تُملأ في أسبوع الإثبات
