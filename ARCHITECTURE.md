# البنية — مدونة ZERO 2 ONE

> العقد الحالي بين المحتوى والبناء والموقع. هذا الملف يصف ما يعمل فعليًا، لا
> مسار SFTP القديم في خطة التنفيذ.

آخر تحديث: 2026-08-04 · الحالة: **خط الإنتاج والقوالب يعملان؛ T7 مكتمل**.

## 1. الخريطة

```text
الكاتب
  │ حفظ Markdown وصور
  ▼
┌────────────────────┐  workflow_dispatch  ┌────────────────────┐
│ blog-content       │ ──────────────────▶ │ blog-build         │
│ مصدر المقالات      │ BUILD_TRIGGER_TOKEN │ Hugo + قوالب       │
│ وحزم الصور         │ actions:write فقط   │ بوابات + ميزانيات  │
└────────────────────┘                     └─────────┬──────────┘
                                                    │ SITE_REPO_TOKEN
                                                    ▼
                                          ┌──────────────────────┐
                                          │ zero2one-web         │
                                          │ الموقع + blog/ مولّد │
                                          └─────────┬────────────┘
                                                    │ Git sync على main
                                                    ▼
                                          zero2one.sa/blog/
                                                    │ بعد ظهور علامة النشر
                                                    └──▶ IndexNow
```

الفصل الحقيقي ثنائي للمصدر (`blog-content` و`blog-build`) ثم تسليم محدود إلى
مستودع الموقع. `blog-deploy` ليس طبقة ثالثة نشطة؛ هو مستودع عام فارغ ومتقاعد.

هذا المسار يخالف عزل المستودعات المثالي لأن خط المدونة يملك رمز كتابة على
مستودع الموقع. السبب العملي: Hostinger تزامن مستودعًا واحدًا إلى `public_html`
وتحذف ما لا يوجد فيه. عندما كان `/blog/` يُرفع منفصلًا بـFTP، حذفته مزامنة
الموقع في 2026-08-04. التعويض الحالي هو حارس diff إلزامي قبل كل push.

## 2. المستودعات والأدوار

| المستودع | المالك | الدور الحالي | الكتابة المعتادة |
|---|---|---|---|
| `zeroone01z21-alt/blog-content` | حساب المدونة | Markdown وصور فقط | الكاتب والمالك |
| `zeroone01z21-alt/blog-build` | حساب المدونة | Hugo، قوالب، بوابات، Actions | المالك/مراجعة محمية |
| `01team9639-maker/zero2one-web` | حساب الموقع | 22 صفحة رئيسية + `blog/` المولّد | خط الموقع وخط المدونة المحدود |
| `zeroone01z21-alt/blog-deploy` | حساب المدونة | **متقاعد وفارغ** | لا أحد |

كلها عامة بقرار المالك. الكاتب يستطيع رؤية `blog-build` لأنه عام، لكنه لا يملك
صلاحية الكتابة عليه؛ يملك Write على `blog-content` فقط.

إذا دخلت بيانات عميل أو نتائج غير منشورة في المقالات، يجب إعادة تقييم رؤية
`blog-content` قبل إدخالها. تاريخ Git العام دائم حتى لو حُذفت لاحقًا.

## 3. عقد المحتوى

المصدر الملزم هو `controls/schema.json`. يقرأه `tools/check_content.py` قبل
كل بناء. إعداد Sveltia سيُولّد منه في T13؛ لا يوجد إعداد لوحة نشط يستهلكه الآن.

البنية:

```text
blog-content/content/
├── _index.en.md
├── _index.ar.md
└── blog/<slug>/
    ├── index.en.md
    ├── index.ar.md        # اختيارية؛ المقال أحادي اللغة مسموح
    ├── cover.webp
    └── *.webp
```

الحقول الأساسية: `title`, `description`, `slug`, `date`, `draft`,
`categories`, `featured_image`, `featured_image_alt`. الحدود الدقيقة ورسائل
الخطأ العربية والتصنيفات الستة في JSON، لا في هذا المستند.

قواعد لا تتغير:

- الصور داخل حزمة المقال، لا مكتبة صور عامة.
- لا HTML خام في Markdown.
- لا ترجمة آلية.
- slug المنشور ثابت؛ الحذف التحريري يتحول إلى أرشفة ما لم يقرر المالك خلافه.
- ملفات `_index.*` صفحات قوائم وليست مقالات، لذلك لا يطبق عليها مدقق المقالات.

في خطوة التجهيز، ينسخ سير البناء محتويات `content/blog/` إلى جذر محتوى Hugo؛
لأن `baseURL` يحوي `/blog/` أصلًا، إبقاء قسم اسمه `blog` سيولد `/blog/blog/`.

## 4. الروابط واللغة

| المخرج | الإنجليزية | العربية |
|---|---|---|
| الرئيسية | `/blog/` | `/blog/ar/` |
| المقال | `/blog/{slug}/` | `/blog/ar/{slug}/` |
| RSS | `/blog/index.xml` | `/blog/ar/index.xml` |
| خريطة اللغة | `/blog/en/sitemap.xml` | `/blog/ar/sitemap.xml` |

`/blog/en/sitemap.xml` اسم ملف تنظيمي يولده Hugo، وليس دليلًا على وجود صفحات
إنجليزية تحت `/blog/en/`. `disableDefaultSiteRedirect = true` يمنع توليد
`/blog/en/index.html` كتحويل JavaScript ناقص metadata. الإنجليزية أصلًا في
الجذر، والعربية وحدها ذات بادئة.

كل صفحة مترجمة تعلن `hreflang` متبادلًا، و`x-default` يشير للإنجليزية.
الروابط بلا تاريخ وبلا تصنيف حتى لا تنكسر عند تحديث المقال أو إعادة تصنيفه.

## 5. البناء والنشر

`build.yml` يتكون حاليًا من 15 خطوة:

1. checkout لقوالب البناء.
2. checkout عام للمحتوى.
3. بصمات الضوابط.
4. تدقيق المحتوى.
5. تنزيل Hugo Extended 0.164.0 والتحقق من SHA-256.
6. تجهيز محتوى Hugo.
7. البناء المصغر.
8. فحص الخرائط وRSS وIndexNow.
9. ميزانيات الأداء.
10. استنساخ `zero2one-web`.
11. استبدال `site/blog/` وحده.
12. جمع روابط IndexNow ووضع علامة نشر فريدة عند وجود روابط متغيرة.
13. حارس النطاق.
14. commit/push إلى `main` عند وجود diff.
15. انتظار علامة هذا النشر على Hostinger ثم إخطار IndexNow.

`concurrency.group = build` و`cancel-in-progress = true`. القياس العملي:
10 حفظات متتابعة أنتجت 5 بناءات بدل 10. هذا تقليل ازدحام، لا ضمان «بناء واحد».

إدخال `reason` من `workflow_dispatch` يمر عبر متغير بيئة قبل أمر Git؛ لا يوضع
GitHub expression مباشرة في shell. السبب أمان مهم لأن خطوة الدفع تحمل
`SITE_REPO_TOKEN`.

### لماذا IndexNow غير حاجب للنشر

كل البوابات حتى الدفع حاجبة. بعد push قد تتعطل خدمة IndexNow أو تتأخر Hostinger؛
لهذا خطوة الإخطار `continue-on-error`. HTML والخرائط المنشورة تبقى سليمة، ومحرك
البحث يستطيع اكتشافها طبيعيًا. الفشل يجب أن يظهر في سجل Actions للمراجعة.

الأداة لا تنتظر مجرد وجود مفتاح قديم. تكتب `indexnow-deploy.txt` بمعرّف فريد،
ثم لا ترسل الإخطار حتى يظهر **المعرّف نفسه** على الموقع. كل نشر يرسل جرد الخرائط
الحالي مع الروابط المحذوفة. وإذا أُلغي تشغيل بعد push وقبل الإخطار، يعيد التشغيل
التالي الجرد مربوطًا بعلامة النشر السابقة من دون صناعة commit فارغ.

## 6. الفهرسة

Hugo يولّد تلقائيًا:

- `/blog/sitemap.xml`: فهرس جامع.
- `/blog/en/sitemap.xml` و`/blog/ar/sitemap.xml`: خريطة لكل لغة.
- `/blog/index.xml` و`/blog/ar/index.xml`: RSS مترجم لكل لغة.

`tools/check_indexing.py` يثبت قبل النشر:

- وجود المخرجات الخمسة وصحة XML.
- أن فهرس الخرائط يشير إلى اللغتين فقط.
- أن الروابط ضمن `/blog/` ومسارات اللغة صحيحة.
- أن `hreflang` متبادل بين الخرائط.
- أن عناصر RSS موجودة في خريطة لغتها.
- أن صفحة التحويل `/blog/en/` غير مولدة.
- أن ملف مفتاح IndexNow العام وصل إلى المخرج وتطابق مع المصدر.

`robots.txt` في جذر الموقع لا يولده Hugo لأن المدونة لا تملك الجذر. بعد نشر T7
يضيف المالك، ضمن T2، السطر:

```text
Sitemap: https://zero2one.sa/blog/sitemap.xml
```

## 7. الأمان والضوابط

| الطبقة | الآلية | الحد الحقيقي |
|---|---|---|
| فصل المصدر | الكاتب يكتب في `blog-content` فقط | لا يعدل workflow أو بوابات البناء |
| رمز التشغيل | `BUILD_TRIGGER_TOKEN`: `actions:write` على `blog-build` | يشغّل workflow ولا يكتب القوالب |
| رمز الموقع | `SITE_REPO_TOKEN`: `contents:write` على `zero2one-web` | **ليس محصورًا بمجلد**؛ الحارس تعويض |
| حارس النطاق | `check_deploy_scope.py` | يفشل لأي diff خارج `blog/` |
| بصمات | `check_controls.py` — 14 ملف ضابط بعد T7 | تكشف التغيير المقصود؛ لا تمنعه وحدها |
| حماية الفرع | CODEOWNERS/قواعد GitHub | الطبقة التي تجعل مراجعة الضوابط إلزامية |
| محتوى | Goldmark `unsafe = false` + مدقق schema | يمنع HTML الخام والنشر الناقص |
| إصدارات | Hugo وActions مثبتة بإصدار/SHA | يمنع انجراف الاعتماديات الصامت |

البصمة «تلاعب مكشوف» لا «تلاعب مستحيل»: من يملك الكتابة يستطيع تعديل الملف
وبصمته معًا، لكن الـdiff يكشف القرار. حماية الفرع والمراجعة هما حد المنع.

أسرار FTP الأربعة في `blog-build` ميتة ويجب حذفها (T5). حساب
`u497805864.blogdeploy` ميت ويزيله المالك (T4). حساب `previewdeploy` يبقى
للمعاينة المستقبلية. الرمزان `BUILD_TRIGGER_TOKEN` و`SITE_REPO_TOKEN` يحتاجان
تدويرًا عاجلًا لأن قيمتيهما ظهرتا سابقًا في محادثة (T6b).

## 8. الأرقام المقيسة

آخر بناء نظيف محلي بـHugo 0.164.0 ومحتوى الإثبات:

| القياس | النتيجة | الحد |
|---|---:|---:|
| CSS مضغوط Brotli | 2.3 KB | 20 KB |
| خطوط عربية | 56.4 KB | 110 KB |
| خطوط إنجليزية | 45.9 KB | 110 KB |
| أثقل صورة | 27.7 KB | 500 KB |
| أثقل مسار حرج | 77.6 KB | 220 KB |

بناء Actions الكامل قيس سابقًا بنحو 50 ثانية. بعد دمج المدونة، تدقيق الموقع
يمر على 28 صفحة HTML (22 للموقع + 6 للمدونة/404 بلغتين) بلا ERROR أو WARN.

## 9. التراجع والنسخ الاحتياطي

التراجع لا يستخدم SFTP ولا `blog-deploy`:

1. استخرج `blog/` من آخر commit سليم في `zero2one-web` وضعه في commit تراجع
   جديد؛ لا تستخدم reset ولا تعِد كتابة تاريخ `main`.
2. شغّل حارس النطاق وفحوص الموقع ثم ادفع.
3. تحقق خارجيًا من الصفحات الحية والتحويلات. لا تعتبر نجاح Git دليل نشر.

`mirror.yml` ينسخ أسبوعيًا `blog-content` و`blog-build` و`blog-deploy` إلى
Codeberg؛ الثالث إرث فارغ إلى أن يقرر المالك حذفه من المصفوفة والمستودعات.
هذه المرآة لا تشمل `zero2one-web`. المصدران يكفيان لإعادة توليد المدونة، لكن
النسخة المحلية الشهرية ونسخة القرص الربع سنوية ما زالتا مطلوبتين في T18.

## 10. افتراضات ثبت خطؤها

- SFTP ليس مسار الإنتاج؛ مزامنة الموقع تحذف ما لا يوجد في مستودعه.
- `blog-deploy` ليس مخزن التراجع ولا صورة لما هو حي.
- `repository_dispatch` احتاج صلاحية أوسع؛ المسار العامل هو
  `workflow_dispatch` مع `actions:write`.
- الكاتب يرى المستودع العام، لكن الرؤية ليست صلاحية كتابة.
- Sveltia لا تدعم فروع Editorial Workflow؛ المسودات حقل Hugo.
- الموقع إنجليزي في الجذر، لا عربي.
- حجم CSS الخام ليس ميزانية نقل؛ القياس بعد Brotli.
- نجاح `set -e` أو push لا يثبت وصول Hostinger؛ يلزم تحقق خارجي/علامة نشر.

## 11. التالي

برمجيًا: T8 صفحات التصنيفات، T9 مدقق SEO العام، ثم T10–T11. مهام المالك
الحاجبة موثقة في `HANDOFF.md`: Search Console، robots، استرداد الكاتب، تدوير
الرموز، وتنظيف FTP. لا تنفّذ مهمة مالك بالنيابة عنه دون تفويض جديد صريح.
