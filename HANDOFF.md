# وثيقة التسليم — موقع ومدونة ZERO 2 ONE

> **لمن يكمل العمل من هنا.** آخر تحديث: 2026-08-04 (استكمال Codex)
>
> اقرأ هذه الوثيقة كاملة قبل أي تنفيذ. القسم **«أخطاء لا تُكرَّر»** يوفّر
> يومًا كاملًا من التشخيص، وكل بند فيه كلّف وقتًا حقيقيًّا ليُكتشَف.

---

## 1. الخريطة الكاملة

### الحسابات

| الحساب | الدور | ملاحظة |
|---|---|---|
| `01team9639-maker` | يملك مستودع الموقع | بريد المالك `01team9639@gmail.com` |
| `zeroone01z21-alt` | يملك مستودعات المدونة الثلاثة | كل أسرار المدونة هنا |
| `zero2one012025-png` | **الكاتب** | Write على `blog-content` فقط. قَبِل الدعوة |
| Codeberg `zero2one` | مرآة النسخ الاحتياطي | ثلاثة مستودعات |
| Hostinger `u497805864` | الاستضافة | حساب مُدار عبر `ocs2611@gmail.com` |

### المستودعات

| المستودع | المحتوى | الرؤية |
|---|---|---|
| `01team9639-maker/zero2one-web` | الموقع (22 صفحة) **+ `blog/` المولَّد** | عام |
| `zeroone01z21-alt/blog-content` | مقالات وصور فقط | عام |
| `zeroone01z21-alt/blog-build` | قوالب · إعدادات · سير عمل · بوابات | عام |
| `zeroone01z21-alt/blog-deploy` | فارغ — **لم يعد يُستخدم** بعد تغيير مسار النشر | عام |

> **لماذا الكل عام:** قرار واعٍ من المالك. المحتوى مقالات بلا بيانات
> عملاء. الفائدة غير المقصودة: حماية الفروع وCODEOWNERS مجانية على
> المستودعات العامة (على الخاصة تحتاج باقة مدفوعة)، ودقائق Actions بلا
> حدود. **يُعاد النظر إن ضمّ المحتوى دراسات حالة بأسماء عملاء.**

### الاستضافة

```
Hostinger مشترك · u497805864 · 92.113.24.24
public_html/          ← مزامنة Git من zero2one-web على main (تلقائية)
public_html/blog/     ← مولَّد، يصل عبر نفس المزامنة
public_html/preview/  ← نطاق preview.zero2one.sa · محمي بكلمة مرور
SSH: 65002 (مفعّل) · FTP: 21
النطاق: خوادم أسماء Hostinger + CDN (server: hcdn) — HTML يمرّ DYNAMIC بلا تخزين
```

### الأسرار — الأسماء فقط

| المستودع | السر | الغرض |
|---|---|---|
| `blog-content` | `BUILD_TRIGGER_TOKEN` | رمز دقيق: `actions:write` على `blog-build` وحده |
| `blog-build` | `SITE_REPO_TOKEN` | رمز دقيق: `contents:write` على `zero2one-web` وحده |
| `blog-build` | `CODEBERG_TOKEN` | `write:repository` للمرآة |
| `blog-build` | `FTP_HOST` `FTP_USER` `FTP_PASSWORD` `FTP_DIR` | **حُذفت 2026-08-04** |
| `blog-build` | `PREVIEW_FTP_HOST` `PREVIEW_FTP_USER` `PREVIEW_FTP_PASSWORD` `PREVIEW_FTP_DIR` | مطلوبة للمعاينة، ولم تُضبط بعد |
| `blog-build` | `HEALTHCHECKS_URL` | اختياري؛ لم يُربط بمراقب خارجي بعد |

**حسابات FTP:** `u497805864.blogdeploy` (محصور في `/blog/` — **لم يعد يُستخدم، يُحذف**) ·
`u497805864.previewdeploy` (محصور في `/preview/` — **يبقى**).

---

## 2. ما أُنجز

### أ) الموقع الحالي

| العمل | الأثر |
|---|---|
| **دبوس خريطة خاطئ في 20 صفحة** | كان يشير إلى «أصول لاستعادة البيانات» (`datarecovery-sa.com`) — مشروع سابق. أخطر تناقض NAP ممكن للسيو المحلي. أُزيل من `hasMap` ومن 22 رابطًا مرئيًّا |
| `meta keywords` | حُذفت من 20 صفحة — جوجل يتجاهلها منذ 2009، وبينغ يعدّها إشارة سبام، وكانت تنشر استهدافكم للمنافسين |
| صور المشاركة | `og-cover.jpg` و`og-cover-ar.jpg` 1200×630 (27/28 KB) بدل صورة عمودية 576 KB |
| فيسبوك | رابط `/share/` غير ثابت → الرابط الأساسي الذي يعلنه فيسبوك |
| ساعات العمل | `openingHoursSpecification` السبت–الخميس 10:00–19:00 |
| الأوصاف التعريفية | 5 أوصاف مفرطة الطول قُصّرت؛ الـ20 الآن بين 70 و155 محرفًا |
| ربط الكيان | `provider` و`author` و`publisher` بـ`@id` واحد → `https://zero2one.sa/#organization` |
| **قسم `/work/`** | صفحة أعمال مستقلة بلغتين، بلا تلميح في الصفحة الرئيسية (طلب المالك)، ورابط في الناف بار والقائمة الجانبية على **22 صفحة** |
| `tools/check_provenance.py` | حارس يمنع أي أثر من مشروع آخر. أمسك عودة الدبوس الخاطئ فعليًّا عند نقل `/work/` |
| `tools/verify_deploy.py` | أصلح خطأ حساسية حالة الأحرف — HTTP/2 يرسل `location` صغيرة والأداة تسأل عن `Location`، فأعلنت 8 تحويلات سليمة «معطوبة» |

**حالة صفحات الموقع الرئيسية الـ22 قبل ضم مخرجات المدونة:** 0 أخطاء · 0 تحذيرات ·
GEO EXCELLENT · 48 كتلة JSON-LD صالحة · `verify_deploy` 45 ok · 0 warning ·
0 failure. بعد دمج المخرج الجديد محليًا، تدقيق الصفحات الـ42 يمر أيضًا
بـ0 ERROR و0 WARN؛ صفحات قوائم المدونة والتصنيفات تحمل CollectionPage وBreadcrumbList.

### ب) بنية المدونة

| المكوّن | الملف |
|---|---|
| عقد الحقول | `controls/schema.json` — **المصدر الواحد**: الحقول وقيودها ورسائل خطأ عربية والتصنيفات الستة |
| الميزانيات | `controls/budgets.json` — مع توثيق أن القياس **حجم النقل مضغوطًا** |
| بصمة الضوابط | `tools/check_controls.py` — يبصم workflows والعقود والبوابات الحساسة ونفسه |
| مدقّق المحتوى | `tools/check_content.py` — رسائل عربية للكاتب |
| حارس النطاق | `tools/check_deploy_scope.py` — يمنع لمس ملف خارج `blog/` |
| فحص الميزانيات | `tools/check_budgets.py` — brotli، يفشل عند التجاوز |
| فحص الفهرسة | `tools/check_indexing.py` — خرائط اللغتين وRSS وIndexNow وغياب تحويل `/en/` |
| نطاق الخرائط | `tools/normalize_sitemaps.py` — ينقل خرائط Hugo إلى جذر `/blog/` قبل الفحص |
| إخطار IndexNow | `tools/indexnow.py` — الجرد الحالي + المحذوفات + علامة تؤكد وصول Hostinger |
| الخطوط | `tools/build_fonts.py` — استضافة ذاتية، وزنان ثابتان |
| القوالب | `layouts/` — هيدر وفوتر ومقال وقائمة و404، ملف CSS واحد للاتجاهين |
| الترجمة | `i18n/en.toml` و`i18n/ar.toml` — 21 مفتاحًا |

### ج) سير العمل

```
الكاتب يحفظ في اللوحة
   └→ blog-content: dispatch.yml  (permissions: {})
        └→ workflow_dispatch إلى blog-build مع SHA المحتوى
             └→ build.yml — 23 مرحلة ظاهرة:
                قوالب · محتوى دقيق · CMS · بصمة الضوابط · تدقيق المحتوى
                · Hugo مثبَّت بـsha256 · تجهيز · بناء · SEO
                · فهرسة · ميزانيات · استنساخ الموقع · استبدال blog/
                · IndexNow · 🔒 حارس النطاق · دفع · تحقق حي · تلخيص/مراقبة
                     └→ zero2one-web/blog/
                          └→ مزامنة Hostinger → public_html/blog/

mirror.yml: أسبوعيًّا → Codeberg (matrix ثلاثي، fail-fast معطّل)
controls.yml: على كل دفعة وطلب دمج → بصمة الضوابط
```

### د) بوابة المرحلة 0 — **13 مكتملة · 2 جزئية · 1 معلّقة**

| ✅ | البند | الدليل المقيس |
|---|---|---|
| ✅ | صفر — اتجاه اللغة | الإنجليزية بلا بادئة، العربية ببادئة |
| ✅ | 1 — إثبات SEO | صفحة حية: معاينات واتساب/تويتر/فيسبوك صحيحة · BlogPosting + BreadcrumbList · hreflang متبادل · 182 كلمة بلا جافاسكربت |
| 🔴 | **2 — Search Console** | **لم يُنفَّذ** |
| ✅ | 2ب — سطر robots | أُضيف إلى `zero2one-web/robots.txt` وينتظر دمج فرع الموقع |
| ✅ | 3 — مسودات Hugo | إنتاج 1 مقال · معاينة 2 · نفس المجلد، الفرق `--buildDrafts` |
| ✅ | 4 — مسار النشر | **تغيّر**: عبر مستودع الموقع لا FTP. اختُبر أن المدونة تنجو من نشر الموقع |
| ✅ | 5 — آلية التشغيل | سلسلة كاملة: دفعة → إشارة → بناء → نشر |
| ✅ | 6 — رمز القراءة | سقط: المستودعات عامة |
| ✅ | 7 — ضبط المتزامن | تجميع 30 ثانية في المحتوى؛ الإنتاج والتراجع طابور `queue: max` واحد غير قابل للإلغاء أو الاستبدال |
| 🟡 | 8 — تسجيل الدخول | يحتاج Cloudflare Worker (المرحلة 4) |
| ✅ | 9 — حماية `/admin` | `/preview/` يرجع 401 من العنوانين · bcrypt · `.ht*` محجوبة 403 |
| ✅ | 10 — Cloudflare | **مخالفة واعية**: لا نقل للنطاق |
| 🟡 | 11 — رموز الكاتب | الدعوة قُبلت · **رموز الاسترداد لم تُحفظ** |
| ✅ | 12 — ضغط الصور | حد 500 KB يُفرض في `check_budgets` و`check_content` |
| ✅ | 13 — الخط العربي | عربية 56.4 / إنجليزية 45.9 من 110 KB |
| ✅ | 14 — النسخ الاحتياطي | مرآة Codeberg تعمل، المستودعات الثلاثة متطابقة |

### هـ) الأرقام المقيسة

```
القياس التاريخي القديم  10 حفظات → 5 بناءات (قبل طابور النشر الحالي)
المسار الحرج            78.0 / 220 KB
CSS                      2.6 /  20 KB
خطوط عربية              56.4 / 110 KB   إنجليزية 45.9 / 110
أثقل صورة               27.7 / 500 KB
زمن البناء الكامل       ~50 ثانية
صفحة الموقع الرئيسي     ~1.6 MB  ← المدونة أخف بثلاثين ضعفًا
```

---

## 3. ⛔ أخطاء لا تُكرَّر

### أ) الاستضافة

**`Password Protect Directories` في hPanel أسقطت خادم الويب كله** (2026-08-03).
الأعراض: TLS يُقطع، خطأ 521 من الخارج، بينما FTP وSSH يعملان. عاد فورًا عند
إزالة الحماية. **لا تُستخدم الأداة إطلاقًا.** البديل المطبَّق: `.htaccess`
يدوي داخل مجلد فرعي عبر حساب FTP محصور فيه — أسوأ أثره خطأ في ذلك المجلد.

**`Header` تحتاج `mod_headers` وهي غير مؤكّدة على هذا الخادم.** استُخدمت في
`/preview/.htaccess` بلا تحقق. البديل الآمن: `<meta name="robots" content="noindex">`
داخل الصفحة.

**مزامنة Git تحذف ما ليس في المستودع.** في 2026-08-04 محا نشرٌ للموقع مجلد
`/blog/` كاملًا وكان يُرفع بـFTP. **هذا سبب تغيير مسار النشر بأكمله.**

**حذف `.htaccess` الجذر ضرره صامت:** الموقع يبدو سليمًا بينما `/tools/` صار
عامًّا و5 تحويلات 301 هبطت إلى صفحات بديلة. الاستعادة: عدّل الملف في Git
وادفع — المزامنة تكتبه.

### ب) القياس والتشخيص

**تحقّق من أداة القياس قبل تصديق نتيجتها.** `/dev/tcp` معطوب في بيئة التنفيذ
وفشل حتى على منفذ 443 العامل، فأنتج تشخيص «حجب IP» وهميًّا وأُرسل المالك
للدعم بمعلومة خاطئة. **`nc` و`curl` صحيحان.**

**للتمييز بين «الموقع معطّل» و«عنواني محجوب»:** استخدم وسيطًا خارجيًّا مثل
`https://api.codetabs.com/v1/proxy?quest=<url>`.

**`if curl ... | tail` يقيس `tail` لا `curl`.** أعلن نجاحًا مرتين ورفعٌ فاشل.
**لا تضع أنبوبًا في شرط.** التقط رمز الخروج مباشرة.

**قِس حجم النقل لا الحجم الخام.** على الموقع الرئيسي قِيس CSS خامًا فأعطى
120 KB وقاد لتوصية بتقسيمه؛ المضغوط الحقيقي **17 KB** والتقسيم كان سيضرّ.

**`cd X` بلا `|| exit` كارثة.** المجلد اختفى بين الجلسات فنُفِّذت أوامر Git
في مستودع الموقع وأنشأت كوميتة دخيلة.

**`set -e` لا يوقف كل شيء.** مرّت كوميتة رغم فشل التحقق. **افصل التحقق عن
الدفع:** `check && push`.

### ج) الأدوات والصيغ

**HTTP/2 يُصغّر أسماء الترويسات.** `dict(r.headers)` يفقد عدم الحساسية لحالة
الأحرف فيقرأ 301 سليمة كـ«بلا وجهة».

**TOML: أي مفتاح بعد ترويسة جدول ينتمي إليه.** `disableKinds` بعد
`[permalinks]` صار `permalinks.disableKinds` وأسقط البناء **صامتًا**.
وفي i18n: الترويسة سطر والمفتاح سطر تالٍ.

**Hugo يضع موارد الحزمة في مجلد اللغة الافتراضية وحده.** بناء رابط الصورة
بـ`printf "%s%s" .Permalink $name` يعطي 404 صامتًا على النسخة العربية.
**استخدم `.Resources.GetMatch`.**

**لا تكتب مسارات الأصول يدويًّا.** `/blog/css/...` يكسر أي معاينة لا تطابق
الإنتاج. **استخدم `relURL`.**

**خطوط Google: `wght@400` يعطي ثابتًا و`wght@400;700` يعطي متغيّرًا.**
الثابتان أصغر بـ6.4 KB ويعطيان المطلوب بالضبط.

**لا تُعِد تجزئة ملفات Google.** تجزئة النطاق اللاتيني **كبّرت** الملف من
32.9 إلى 36.8 KB.

### د) GitHub

**`repository_dispatch` يتطلب `contents:write`**، أي تعديل قوالب البناء —
وهو ما وُضع مبدأ أقل صلاحية لمنعه. **`workflow_dispatch` يحتاج `actions:write`
وحدها.** أُثبت: 403 مقابل 204.

**حماية الفروع مجانية على المستودعات العامة فقط.** على الخاصة تحتاج باقة
مدفوعة. اختيار «عام» هو ما جعل القسم 12.6 قابلًا للتنفيذ بلا اشتراك.

**Codeberg تقيّد مرايا السحب التلقائية.** خانة «This repository will be a
mirror» غير متاحة في نموذجَي الهجرة. الحل: سير عمل يدفع إليها.

### هـ) في الخطة نفسها

- **`Sveltia` لا تدعم فروع المسودات** — أُثبت. آلية `draft` في Hugo هي البديل.
- **الموقع إنجليزي في الجذر** لا العربية كما افترض السجل.
- **بادئة المسار أثرها على الترتيب صفر** — المعيار هو اتساق التجربة.
- **صفحات `_index.*` ليست مقالات** — مدقّق يعاملها كمقال يُفشل كل بناء.

---

## 4. 📋 قائمة المهام المتبقية

### ✅ أُنجز في استكمال 2026-08-04 (بانتظار الدمج والنشر الحي)

**[T7] خرائط الموقع وRSS وIndexNow**

- فهرس جامع + خريطة لكل لغة مع `hreflang` متبادل؛ تُنقل خريطتا اللغة إلى
  `/blog/sitemap-en.xml` و`/blog/sitemap-ar.xml` كي لا تصفا روابط أعلى من
  مجلد ملف الخريطة.
- RSS مستقل ومترجم لكل لغة مع autodiscovery في `<head>`.
- مفتاح IndexNow عام داخل `/blog/`، وجرد آلي للروابط الحالية والمحذوفة.
- علامة نشر فريدة تمنع الإخطار قبل وصول مزامنة Hostinger الفعلية.
- بوابة `check_indexing.py` تمنع رجوع تحويل `/blog/en/` غير المرغوب.
- أصلحت صفحات 404 الناقصة، وصار تدقيق تكامل الموقع: **0 ERROR · 0 WARN**.

**[T20] `ARCHITECTURE.md` و`CLAUDE.md`**

أُعيدت كتابتهما لتطابقا مسار `blog-content → blog-build → zero2one-web/blog/`
الحقيقي، وتقاعد `blog-deploy` وSFTP، والأرقام المقيسة الحالية.

**تقوية إضافية:** أُغلق حقن shell كان ممكنًا عبر حقل `workflow_dispatch.reason`
في خطوة تحمل `SITE_REPO_TOKEN`؛ صار الإدخال يمر عبر متغير بيئة قبل أمر Git.

أُنجز أيضًا: **T2، T5، T8–T11، T13، جانب الكود من T14–T16، T17، T18،
T20، T23، وT26**. أُضيف workflow تراجع لـT19 واختُبرت خطواته محليًا؛ قياس
تراجع حي فعلي بقي قرار تشغيل للمالك، لأنه يصنع نشرًا حقيقيًا إضافيًا.

### 🔴 عاجل — يحجب الإطلاق

**[T1] Search Console — بند 2** · 🧑 المالك
1. [search.google.com/search-console](https://search.google.com/search-console) ← Add property ← **Domain** ← `zero2one.sa`
2. انسخ نص `google-site-verification=…`
3. hPanel ← Advanced ← **DNS Zone Editor** ← سجل `TXT` باسم `@`
4. ⛔ **لا تلمس سجل `v=spf1`** — هو بريد الشركة. أضف سجلًا جديدًا بجانبه
5. ارجع لجوجل ← Verify (قد يحتاج 10–30 دقيقة)

**[T2] سطر robots — بند 2ب** · ✅ أُنجز
أُضيف في `zero2one-web/robots.txt` وينشر مع فرع الموقع:

```text
Sitemap: https://zero2one.sa/blog/sitemap.xml
```

**[T3] رموز استرداد الكاتب — بند 11** · 🧑 المالك
الكاتب (`zero2one012025-png`) يفعّل التحقق الثنائي ويسلّم رموز الاسترداد
للمالك ليحفظها في مدير كلمات مرور.

---

### 🟠 تنظيف — يزيل مخاطر قائمة

**[T4] حذف حساب FTP الميت** · 🧑 المالك
hPanel ← FTP Accounts ← احذف **`u497805864.blogdeploy`**. **أبقِ `previewdeploy`.**

**[T5] حذف أسرار FTP الميتة** · ✅ أُنجز
حُذفت `FTP_HOST` `FTP_USER` `FTP_PASSWORD` `FTP_DIR` من أسرار `blog-build`.

**[T6] حذف المستودعات القديمة** · 🧑 المالك
`zero2one012025-png/zero2one_blog` · `/blog-build` · `/blog-deploy` — فارغة،
وفيها رمز Codeberg قديم يجب إبطاله.

**[T6b] إبطال الرموز المكشوفة** · 🧑 المالك
`BUILD_TRIGGER_TOKEN` و`SITE_REPO_TOKEN` مرّا في محادثة. أبطلهما وأنشئ
بديلين بنفس الصلاحيات، وحدّث السرّين.

---

### 🟡 إكمال الواجهة

**[T8] صفحات التصنيفات** · ✅ أُنجز
ستة أرشيفات ثنائية اللغة من `controls/schema.json`، مع وصف ومقدمة وروابط
مفهرسة. بقيت القائمة ثابتة عمدًا بدل إنشاء تصنيفات رقيقة من لوحة الكاتب.

**[T9] فحص SEO آلي — القسم 8.2** · ✅ أُنجز
`tools/check_seo.py` يفرض metadata وcanonical وalt والعناوين وhreflang وJSON-LD؛
أضيف إلى الإنتاج، وصُحح تدقيق الموقع لصفحات 404 والتحويل الدائم.

**[T10] صفحة 404 على مستوى الخادم** · ✅ أُنجز
`.htaccess` للغتين يثبت 404 الصحيحة و301 من `/blog/en/` ومواضع 410 الموثقة،
بلا `Header`، واختُبر على Apache 2.4.

**[T11] الأصول المتجاوبة** · ✅ أُنجز
AVIF/WebP بأحجام 480/800/1200 وfallback و`width`/`height` وlazy/fetchpriority.

---

### 🟢 لوحة الكاتب — المرحلة 4

**[T12] وسيط المصادقة** · ✅ أُنجز خارجيًا وربط بالكود
نُشر [`sveltia-cms-auth`](https://github.com/sveltia/sveltia-cms-auth) على
`https://sveltia-cms-auth.01team9639.workers.dev`، وضُبط GitHub OAuth callback
والسر المشفّر و`ALLOWED_DOMAINS=preview.zero2one.sa`. اختُبر تحويل النطاق
المسموح ورفض نطاق آخر. لا يُستخدم «الدخول برمز»؛ تبقى دورة الدخول الكاملة
ضمن اختبار T15 بعد رفع اللوحة.

**[T13] Sveltia مستضافة ذاتيًّا** · ✅ أُنجز برمجيًا
Sveltia v0.179.0 محلية ومبصومة، و`config.yml` مولّد من العقد، وحذف/نسخ المقال
معطلان، والصور والنص البديل والحالات مضبوطة. توجد مجموعة `posts` واحدة؛
التصنيفات خيارات ثابتة من المخطط لمنع مصدر حقيقة ثانٍ وصفحات SEO رقيقة.

**[T14] إعادة حماية `/preview/`** · 🟡 الكود جاهز؛ التطبيق الحي ينتظر FTP
كل صفحات المعاينة واللوحة `noindex`. عند أول رفع، يسحب workflow ملف الحماية
العامل، يثبت بقاء Basic Auth، يحذف `Header` فقط، ويعيده. لا يلمس `.htpasswd`.

**[T15] بناء المعاينة** · 🟡 الكود جاهز؛ محجوب خارجيًا
`preview.yml` يبني drafts على `/blog/` ويرفع الجذر والمدونة و`/admin/` بـFTPS.
OAuth مضبوط؛ يتوقف الرفع عمدًا حتى يضيف المالك أسرار `PREVIEW_FTP_*` الأربعة.

---

### 🔵 التشغيل والمراقبة

**[T16] مراقب خارجي — القسم 14.1** · 🟡 الربط فقط بقي للمالك
الإنتاج والتراجع يرسلان start/success/fail اختياريًا عبر `HEALTHCHECKS_URL`،
والتحقق الحي من علامة الإصدار حاجب. أنشئ check خارجيًا واضبط السر لتفعيل التنبيه.

**[T17] التنبيهات للطرفين — القسم 14.2** · ✅ أُنجز
ملخص build يفرّق المحتوى عن العطل التقني بالعربية/الإنجليزية. Workflow المحتوى
يتابع تشغيل SHA نفسه ويعرض للكاتب النتيجة والرابط داخل `blog-content`.

**[T18] النسخة المحلية** · ✅ أُنجز واختُبر
`python3 tools/backup_local.py <destination>` ينسخ الموقع والبناء والمحتوى
كـmirrors ويشغل `git fsck` ويكتب manifest. اختُبر على المستودعات الثلاثة.

**[T19] توثيق إجراء التراجع** · 🟡 workflow جاهز ومحاكى
`rollback.yml` يستعيد SHA سليمًا من تاريخ main في commit جديد، يحرس النطاق،
يدفع وينتظر العلامة الحية. لم يُنفّذ تراجع حي عبثي لمجرد القياس؛ قِس زمنه عند
أول حاجة حقيقية أو نافذة اختبار يوافق عليها المالك.

**[T25] توحيد ساعات العمل** · 🧑 قرار + 🤖 تنفيذ
JSON-LD يقول السبت–الخميس 10:00–19:00، بينما `llms.txt` يقول
الأحد–الخميس 09:00–18:00، والوصف العربي يقول الأحد–الخميس. يقرر المالك الصحيح
مرة واحدة، ثم تُوحّد المصادر المولّدة والمرئية؛ **لا تخمّن الجدول**.

**[T26] تثبيت esbuild في الموقع** · ✅ أُنجز
ثُبت esbuild 0.28.1 وPlaywright 1.62.1 وSharp 0.35.3 بإصدارات دقيقة؛
`npm audit` صفر، والبناء والانحدار البصري 44/44 بلا فرق.

---

### ⚪ المحتوى — قرارات المالك

**[T21] حذف صفحة الإثبات** · 🧑 قرار
`/blog/pipeline-proof/` بلغتيها. مُعلَّمة كفحص تقني. تُحذف متى شئت.

**[T22] المقال الأول** · 🧑 + 🤖
منشور حاليًّا على `zero2onemarketing.blogspot.com` (عربي · ~2000 كلمة ·
أسباب فشل الحملات الإعلانية · بلا أسماء عملاء).
⚠️ **نشره دون معالجة = الموقع ينافس نفسه عبر نطاقين.**
**قبل النشر:** احذفه من blogspot أو ضع فيه `rel="canonical"` يشير إلينا.

**[T23] دليل الكاتب** · ✅ كُتب؛ اختبار الكاتب بقي بشريًا
`WRITER_GUIDE_AR.md` يغطي الدخول والكتابة والصور والتصنيفات والحالات والمعاينة
والنشر ورسائل العقد وفقدان الوصول. يراجعه الكاتب عمليًا بعد تفعيل OAuth.

**[T24] PDPL** · 🧑 قرار
GA4/GTM يعملان على صفحات الموقع الرئيسية **بلا بوابة موافقة** — مخالفة واعية
موثّقة. المدونة الجديدة لا تحمل JavaScript تحليلات طرف ثالث حاليًا، التزامًا
بقرار «لا موافقة = لا سكربت». بانر على المدونة وحدها لا يعالج الموقع؛
**معالجة الموافقة مشروع للموقع كله، وبعدها يُفعّل GA4 نفسه للمدونة.**

---

## 5. كيف تُكمل

### أوامر يومية

```bash
# المدونة (blog-build)
python3 tools/prepare_content.py ../blog-content/content  # تجهيز واستبعاد المؤرشف
python3 tools/check_content.py content     # تدقيق الناتج
python3 tools/check_cms.py                 # عقد اللوحة والنسخة المحلية
python3 tools/check_controls.py            # بصمة الضوابط
python3 tools/check_controls.py --update   # بعد تعديل ضابط مشروع
hugo --gc --minify --destination public    # بناء
python3 tools/normalize_sitemaps.py public  # تصحيح نطاق خرائط اللغات
python3 tools/check_budgets.py public      # الميزانيات
python3 tools/check_indexing.py public     # الخرائط وRSS ومفتاح IndexNow
python3 tools/check_seo.py public          # metadata/hreflang/schema والخادم
python3 tools/check_deploy_scope.py site   # حارس النطاق
python3 tools/indexnow.py collect site --output /tmp/indexnow.json \
  --deployment-id local-check             # الجرد الحالي + المحذوفات + علامة النشر
python3 tools/indexnow.py submit /tmp/indexnow.json --dry-run
python3 tools/build_fonts.py               # الخطوط
python3 tools/backup_local.py /path/to/existing/backup-disk

# الموقع (zero2one-web)
python3 tools/check_provenance.py          # بقايا مشاريع أخرى
python3 tools/audit.py                     # تدقيق SEO
python3 tools/verify_deploy.py             # الموقع الحي بعد كل نشر
node tools/visual_check.js                 # انحدار بصري
sh tools/build.sh                          # CSS/JS + بصمات ?v=
python3 tools/build_ar.py                  # توليد /ar/
```

### قواعد ملزمة

1. **لا تلمس ملفًا في `zero2one-web` خارج `blog/`** من خط المدونة. الحارس يمنع.
2. **`blog/` مولَّد** — أي تحرير يدوي يختفي مع أول نشر بلا خطأ.
3. **لا ترفع حد ميزانية** لتمرير بناء فاشل. أبلغ المالك.
4. **الأسرار والحسابات وDNS والاستضافة `[المالك]`** — لا تُنفَّذ.
5. **بعد أي تعديل يمسّ إعداد الخادم: افحص الموقع فورًا** وكن مستعدًّا للتراجع.
6. **تعديل ضابط يتطلب `--update` للبصمة في نفس العملية** — وإلا فشل البناء.
7. **افصل التحقق عن الدفع** — `check && push` لا `set -e` وحده.

### الأولوية عند تعارض

```
SEO 30%  ←  تجربة الزائر 20% · الأمان 20%  ←  الاعتمادية 15%
```
**ووثّق القرار.**

### مصادر الحقيقة

| الملف | المستودع |
|---|---|
| `controls/schema.json` | `blog-build` — الحقول والتصنيفات |
| `controls/budgets.json` | `blog-build` — الميزانيات |
| `CLAUDE.md` | `blog-build` — المحظورات المطلقة |
| `ARCHITECTURE.md` | `blog-build` — العقد بين المستودعات |
| `tools/redirects.json` | `zero2one-web` — كل التحويلات |
| هذه الوثيقة | `blog-build` — الحالة والمهام |
