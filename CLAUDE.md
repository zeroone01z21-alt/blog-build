# CLAUDE.md — ضوابط مشروع المدونة

> يُقرأ تلقائياً في كل جلسة. **قيود مطلقة لا اجتهاد فيها.**
>
> مصدر الحقيقة: `blog-decisions-register-v2.2.md` + ملحق v2.3
> التنفيذ: `blog-implementation-plan-v2.md`

---

## نطاق العمل — احفظه

**المدونة تعيش في `/blog/` فقط.**

الموقع الحالي (~20 صفحة) له خط بناء قائم: `build.sh` و `build_ar.py` و
`ar-dictionary.json` و `send.php`، ومستودعه منفصل تماماً
(`01team9639-maker/zero2one-web`) ومربوط بـ Hostinger على `main`.

**Hugo لا يعيد بناء الموقع.** الصفحات العشرون تبقى كما هي بلا أي تعديل.
مهمتنا: **استخراج الهيدر والفوتر والـ CSS إلى قوالب لمطابقة التصميم بصرياً** —
استخراج للقراءة فقط، لا ترحيل ولا تعديل.

---

## ❌ ما لا يُنفَّذ إطلاقاً

### الملفات والنطاق
- ❌ **مسّ أي ملف في الموقع الحالي خارج `/blog/`** — بما فيها `build.sh` و
  `build_ar.py` و `ar-dictionary.json` و `send.php`
- ❌ مسّ جذر النطاق. التعديلان اليدويان المسموحان (`robots.txt` وإنشاء `/blog/`
  فارغاً) **من المالك حصرياً**
- ❌ وضع أي ملف يدوي داخل `/blog/` — النشر يكتب فوقه. كل شيء يمرّ عبر مستودع النشر
- ❌ **فتح شاشة Git في hPanel** — «Change repository» تستبدل الموقع الحي بالكامل

### الأسرار
- ❌ **كتابة أي رمز أو كلمة مرور أو سر في أي ملف، مهما كان السبب**
- ❌ طباعة قيمة أي سر في المخرجات أو السجلات
- ❌ إنشاء رمز بديل عن رمز أُبطل — أبلغ المالك

### الحذف
- ❌ إعادة كتابة تاريخ Git آلياً
- ❌ حذف آلي لأي صورة — **تنبيه فقط**
- ❌ حذف مقالات من اللوحة — **أرشفة فقط**

### البوابات
- ❌ **رفع حد أي ميزانية لتمرير بناء فاشل** — أبلغ المالك بدل ذلك
- ❌ تعديل ملفات الضوابط (الميزانيات، المخطط، سير العمل، هذا الملف) دون موافقة المالك
- ❌ تجاوز فحص الأمان أو التعقيم بأي مبرر
- ❌ تعطيل بوابة لأنها «تعترض شيئاً عاجلاً» — استخدم مسار الطوارئ الموثّق

### البناء
- ❌ نسخ أو تحرك لأي إجراء — **بصمة نسخة كاملة فقط**
- ❌ HTML خام مفعّل في Markdown (`unsafe: true`)
- ❌ جافاسكربت طرف ثالث قبل عرض الصفحة
- ❌ ترجمة آلية للمقالات · `ar-dictionary.json` **لا يُستخدم في المدونة إطلاقاً**

---

## 🔧 الأدوات

**المبدأ: الأداة المتاحة إغراء، لا قدرة محايدة. الإتاحة ليست إذناً.**

**مصرَّح:** `git` · `gh` · `hugo` (إصدار مثبَّت) · أدوات الفحص · Playwright
(لاختبار معاينات الروابط — بديل Claude in Chrome غير المتاح)

**ممنوع صراحةً:**
- **Vercel** — يملك صلاحيات شراء (`buy_domain`, `buy_credits`) ولا علاقة له بالمشروع
- **Supabase / Webflow / Canva / Figma** — مرفوضة أو خارج النطاق
- **Control your Mac / MacOS-MCP** — تحكم كامل بالجهاز، يُلغي حدّ الصندوق الرملي
- **Hostinger API MCP** — التحكم بفتح الحساب كله

**القاعدة النهائية:** أي إجراء يمسّ **الاستضافة أو DNS أو الحسابات أو الأسرار**
— **توقّف واطلب المالك**، حتى لو كانت الأداة متاحة تقنياً.

---

## 📦 قواعد العمل

1. **المراحل متسلسلة.** لا تبدأ مرحلة قبل اجتياز بوابة سابقتها.
2. **بند فشل = توقف.** لا تلتف ولا تخترع بديلاً. أبلغ، واذكر أي قرار في السجل
   يحتاج إعادة فتح.
3. **`[المالك]` لا تُنفَّذ.** الحسابات، كلمات المرور، الرموز، التحقق الثنائي،
   DNS، إعدادات الاستضافة.
4. **لا تخمّن واجهات الأدوات.** إعدادات Hugo و Sveltia و Hostinger تتغيّر —
   **اقرأ التوثيق الرسمي الحالي**.
5. **الأولوية عند تعارض غير متوقع:** SEO (30%) ← تجربة الزائر / الأمان (20% لكلٍّ)
   ← الاعتمادية (15%). **وثِّق القرار في السجل.**

---

## ❓ متى تتوقف وتسأل

- أي إجراء يمسّ الاستضافة أو DNS أو الحسابات أو الأسرار
- بناء يفشل بسبب ميزانية أو بوابة جودة
- تعارض بين قرارين في السجل
- الحاجة لتعديل ملف خارج `/blog/` أو خارج مستودعات المدونة
- اكتشاف أن افتراضاً في السجل غير صحيح
- الحاجة لأداة غير مصرَّح بها

---

## 📕 مسائل محسومة — لا تُطرح ثانية

إعادة فتحها تتطلب سبباً جديداً موثَّقاً (التفصيل في الملحق «ب» من السجل):

| السؤال | الجواب |
|---|---|
| Supabase؟ | SEO ينهار — 28.4% مرجَّح مقابل 91.6% |
| WordPress؟ | عبء أمان دائم، أبطأ، فقدان السيطرة على التصميم |
| Astro؟ | مئات حزم npm واعتماديات تتعفّن |
| توسيع `build_ar.py` بدل Hugo؟ | يعني كتابة مولّد مواقع من الصفر وصيانته وحدنا |
| فروع للمسودات؟ | **Sveltia لا تدعمها — أُثبت عملياً** |
| حذف آلي للصور؟ | حزم الصفحة تُلغي الحاجة، والحذف يخاطر بصورة حية |
| Umami مع GA4؟ | أداتان = سكربتان بلا فائدة إضافية |
| `/en/blog/` بدل `/blog/en/`؟ | يتطلب مدى نشر يلامس الجذر، وأثره على SEO صفر |
| توجيه Cloudflare لتجميل الروابط؟ | يجعل الحالة حاملة للروابط، تعطيلها = سقوط لغة كاملة |
| تاريخ في الرابط؟ | يجعل المقال يبدو قديماً ويمنع تحديثه |
| ترجمة آلية؟ | صفر — الكاتب يكتب النسختين أو ينشر بلغة واحدة |

---

## ✅ قرارات مستقرة (الملحق v2.3 — 2026-08-02)

- **اتجاه اللغة:** الإنجليزية بلا بادئة `/blog/{slug}/` · العربية `/blog/ar/{slug}/`
- **الرؤية:** المستودعات الثلاثة **عامة** — قرار واعٍ. يُعاد النظر إن ضمّ المحتوى
  أسماء عملاء أو أرقام نتائج
- **النشر:** SFTP بحساب مقيَّد على `/blog/`. **ربط Git الأصلي ساقط نهائياً**
- **Cloudflare:** لا يُنقل النطاق. Worker مجاني للمصادقة فقط، لا يمسّ DNS
- **GA4:** نفس معرّف الموقع، بلا بوابة موافقة — مخالفة واعية للقسم 18، ومبرَّرها
  في الملحق. PDPL فجوة مفتوحة على مستوى الموقع كله
- **الخطوط:** Cairo للعربية · NeueMontreal للإنجليزية · مستضافة ذاتياً
- **لا اشتراكات مدفوعة**
