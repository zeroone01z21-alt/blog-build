# CLAUDE.md — ضوابط مشروع مدونة ZERO 2 ONE

> تعليمات تشغيل ملزمة لأي وكيل أو مطوّر يعمل على المدونة.
>
> آخر تحقق: 2026-08-04. هذه الوثيقة تصحّح وصف النشر القديم المبني على SFTP.

## مصادر الحقيقة

عند التعارض اتبع هذا الترتيب، ولا تدمج تفسيرين متناقضين:

1. `controls/schema.json` و`controls/budgets.json` للعقود القابلة للفحص.
2. `.github/workflows/build.yml` و`ARCHITECTURE.md` لمسار البناء والنشر الحالي.
3. `HANDOFF.md` للحالة المقيسة والمهام المتبقية والأخطاء المعروفة.
4. سجل القرارات وخطة التنفيذ v2 وثيقتان تاريخيتان؛ لا يُعمل ببند صحّحته
   الوثائق الأحدث أو أثبت التنفيذ خطأه.

إذا خالف التنفيذ هذه المصادر، أوقف النشر وصحّح الوثيقة والتنفيذ في العملية
نفسها. لا تجعل السلوك الفعلي «تصحيحًا صامتًا» للعقد.

## نطاق العمل

المدونة تعيش في `/blog/` فقط. موقع `01team9639-maker/zero2one-web` يحوي 22
صفحة للموقع الرئيسي إضافة إلى `blog/` المولّد. Hugo يبني المدونة وحدها ولا
يعيد بناء صفحات الموقع الرئيسية.

- المصدر التحريري: `zeroone01z21-alt/blog-content`.
- القوالب والبوابات: `zeroone01z21-alt/blog-build`.
- المخرج الحي: `zero2one-web/blog/`، ثم مزامنة Hostinger الموجودة أصلًا.
- `blog-deploy` فارغ ومتقاعد؛ ليس طبقة نشر أو تراجع.

يجوز قراءة الموقع الرئيسي واختباره. لا يحق لخط المدونة تغيير أي ملف في
`zero2one-web` خارج `blog/`. أي عمل مقصود خارج هذا الحد مشروع مستقل يحتاج
موافقة صريحة وفحوص الموقع الخاصة به.

`zero2one-web/blog/` مخرج مولّد. لا تحرره يدويًا؛ التعديل الصحيح يبدأ من
`blog-build` أو `blog-content` ثم يعاد البناء.

## عقد النشر الحالي

```text
blog-content
  └─ workflow_dispatch باستخدام BUILD_TRIGGER_TOKEN
      └─ blog-build: فحص + Hugo + ميزانيات
          └─ استنساخ zero2one-web باستخدام SITE_REPO_TOKEN
              └─ استبدال site/blog/ فقط
                  └─ حارس النطاق ثم push إلى main
                      └─ Hostinger Git sync
                          └─ zero2one.sa/blog/
```

لا تغيّر ربط Git في hPanel ولا تربطه بـ`blog-build` أو `blog-deploy`. Hostinger
تقبل مستودع الموقع الحالي، ومزامنتها تحذف الملفات غير الموجودة فيه؛ لهذا صار
`blog/` جزءًا من `zero2one-web` بدل الرفع المنفصل بـFTP.

`SITE_REPO_TOKEN` قادر تقنيًا على كتابة المستودع كله، لأن GitHub لا يقيّد الرمز
بمجلد. `tools/check_deploy_scope.py` تعويض دفاعي إلزامي، لا حد صلاحية حقيقي.

## محظورات مطلقة

### الأسرار والحسابات

- لا تكتب رمزًا أو كلمة مرور أو سرًا في ملف أو سجل أو تعليق.
- لا تطبع عنوان remote يحوي رمزًا. أخفِ بيانات الاعتماد في كل مخرج.
- لا تنشئ أو تدوّر رمزًا، ولا تغيّر DNS أو Hostinger أو إعداد حساب، دون المالك.
- `BUILD_TRIGGER_TOKEN` و`SITE_REPO_TOKEN` ظهرا سابقًا في محادثة؛ تدويرهما
  مهمة مالك عاجلة. لا تعتبر وجودهما الحالي دليل أمان.
- مفتاح IndexNow النصي **عام حسب البروتوكول** وليس سرًا. يبقى مع ذلك محصورًا
  تحت `/blog/` ولا يُعاد استخدامه لنطاق آخر.

### الملفات والحذف

- لا تمس ملفًا خارج `site/blog/` أثناء نشر المدونة.
- لا تعِد كتابة تاريخ Git، ولا تحذف صورًا آليًا، ولا تحذف مقالات من اللوحة؛
  الأرشفة هي المسار التحريري الآمن.
- لا تعدّل مخرج `blog/` يدويًا ولا تضف إليه ملفًا لا يولده خط البناء.
- لا تحذف حساب FTP `previewdeploy`؛ الحساب الميت المرشح للحذف هو
  `u497805864.blogdeploy` فقط، ومن المالك.

### البناء والجودة

- لا ترفع ميزانية لتمرير بناء فاشل.
- لا تتجاوز فحص المحتوى أو الأمان أو النطاق أو الفهرسة.
- لا تحدّث بصمة ضابط إلا مع تعديل مقصود ومراجع للضابط نفسه.
- لا تستخدم وسمًا متحركًا لإجراء GitHub؛ ثبّت SHA كاملًا.
- لا تفعّل HTML الخام في Markdown (`unsafe = true`).
- لا تضف JavaScript لطرف ثالث قبل عرض الصفحة.
- لا تستخدم `ar-dictionary.json` ولا ترجمة آلية للمقالات.
- لا تغيّر slug منشورًا؛ الرابط الدائم لا يتغير.

## بوابات كل بناء إنتاج

الترتيب الملزم في `build.yml`:

1. بصمات الضوابط.
2. عقد المحتوى ورسائل الكاتب.
3. Hugo 0.164.0 Extended بعد تحقق SHA-256.
4. بناء HTML ثابت.
5. خرائط اللغتين وRSS وتهيئة IndexNow.
6. ميزانيات النقل المضغوط.
7. استبدال نسخة `site/blog/` المحلية.
8. جمع روابط IndexNow ووضع علامة نشر فريدة.
9. حارس يمنع أي diff خارج `blog/`.
10. الدفع إلى موقع `main`.
11. انتظار علامة النشر الحية ثم إخطار IndexNow. فشل الإخطار لا يلغي نشرًا
    تم بنجاح، لكنه يجب أن يبقى ظاهرًا في سجل Actions.

فشل أي بوابة قبل الدفع يعني: لا نشر. لا تعتمد على `set -e` وحده؛ افصل التحقق
عن الدفع واختبر رمز الخروج الحقيقي دون أنبوب يخفيه.

## قرارات مستقرة

| الموضوع | القرار |
|---|---|
| اللغة | الإنجليزية `/blog/{slug}/`، العربية `/blog/ar/{slug}/` |
| الرابط | بلا تاريخ وبلا تصنيف؛ لا `/blog/en/` لصفحات المحتوى |
| المحتوى | حزمة صفحة، صورة داخل الحزمة، ترجمة اختيارية في الحزمة نفسها |
| المسودات | Hugo `draft`; Sveltia لا تدعم Editorial Workflow |
| التصنيفات | معطلة حتى T8، وأسماؤها من `controls/schema.json` |
| التحليلات | GA4 نفسه؛ فجوة الموافقة/PDPL تخص الموقع كله ومعلّقة بقرار المالك |
| الخطوط | Cairo للعربية وNeueMontreal للإنجليزية، مستضافة ذاتيًا |
| Cloudflare | لا نقل للنطاق؛ Worker المصادقة لاحقًا فقط |
| الكلفة | لا اشتراكات مدفوعة |

المستودعات عامة بقرار واعٍ. إذا دخلت أسماء عملاء أو أرقام نتائج في المحتوى،
يُعاد تقييم الرؤية قبل الحفظ، لا بعد النشر.

## متى تتوقف وتطلب المالك

- نشر/دمج/دفع بعيد لم يأذن به صراحة.
- أي عمل يمس الاستضافة أو DNS أو الحسابات أو الأسرار أو حماية الفروع.
- تعديل مطلوب خارج نطاق المدونة.
- بوابة جودة فاشلة أو تعارض بين مصدرَي حقيقة.
- إجراء حذف مادي أو قرار محتوى لا يمكن التراجع عنه بسهولة.

## الحالة عند هذا التحديث

- القوالب وخط الإنتاج يعملان، وصفحة الإثبات موجودة بلغتين.
- T7 مكتمل في هذا التغيير: خرائط لغة وفهرس جامع، RSS مترجم، IndexNow، وفحص
  يمنع رجوع صفحة التحويل `/blog/en/` أو كسر عقد الفهرسة.
- التالي برمجيًا: T8 صفحات التصنيفات، ثم T9 بوابة SEO العامة.
- قبل الإطلاق ما زالت هناك مهام مالك: Search Console، سطر robots، رموز استرداد
  الكاتب، تدوير الرمزين المكشوفين، وحذف حساب/أسرار FTP الميتة.
